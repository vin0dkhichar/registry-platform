global:
  # THE single source of truth for every public hostname in this chart. Override
  # it once at install and everything below follows -- nothing should spell out a
  # domain of its own, or changing environment means hunting a dozen values and
  # missing one.
  #
  # Matches the commons-services convention, so a stack installed with the same
  # baseDomain lines up across charts.
  baseDomain: '{{ .Release.Namespace }}.openg2p.org'

  registryHostname: '{{ .Release.Name }}.{{ tpl .Values.global.baseDomain $ }}'

  # Database
  postgresqlHost: commons-postgresql

  registryDB:  '{{ printf "%s" .Release.Name | replace "-" "_" }}'
  registryDBPort: 5432
  registryDBUser: '{{ printf "%s_user" .Release.Name | replace "-" "_" }}'
  registryDBSecret: '{{ .Release.Name }}'
  registryDBUserPasswordKey: '{{ .Release.Name }}-db-user'

  # Master Data DB credentials — used by the registry db-seed Job when it
  # loads geo into the Master Data database. Runtime registry pods talk to
  # Master Data over HTTP (masterDataApiUrl), not this database.
  masterDataDB: 'master_data'
  masterDataDBPort: 5432
  masterDataDBUser: 'master_data_user'
  # Must be 'master-data-db', NOT 'master-data': keycloak-init names its
  # client-secret Secret after the clientId, and a 'master-data' Keycloak client
  # exists. Both templates create-if-absent with resource-policy keep, so
  # whichever ran first won and the other silently skipped — leaving a Secret
  # holding only client_secret. Every consumer then failed to start with
  # "couldn't find key master-data-db-user in Secret master-data".
  # commons-services renamed it; this must match.
  masterDataDBSecret: 'master-data-db'
  masterDataDBUserPasswordKey: 'master-data-db-user'

  # Redis
  redisInstallationName: '{{ .Release.Name }}-redis-master'

  # MinIO — commons-minio: root-user/root-password + readonly-user/readonly-password
  minioSecret: commons-minio
  minioAccessKey: root-user
  minioSecretKey: root-password
  minioReadonlyAccessKey: readonly-user
  minioReadonlySecretKey: readonly-password
  # External MinIO S3 API endpoint. Must be reachable from end-user browsers
  # because the registry generates pre-signed S3 URLs that browsers follow
  # directly. Commons-base's Istio VirtualService exposes the S3 API at
  # `minio-api.<basedomain>` (apiHost in commons-base values.yaml). The
  # default below is a placeholder using the namespace + openg2p.org —
  # OPERATORS MUST override this in `questions.yaml` / Rancher UI / values
  # file to match their actual base domain. NOTE: `minio.<basedomain>` is
  # the *Console* UI — do NOT point S3 clients at it.
  minioHost: 'minio-api.{{ tpl .Values.global.baseDomain $ }}'
  # MinIO buckets — hard-set to match DocumentBucket enum values used by
  # the platform document handler (physical bucket name == enum value).
  # Catalog rows store bucket as 'templates' / 'documents' / 'default'.
  defaultBucketName: 'default'
  templateBucketName: 'templates'
  # Registrant photos / record images land in the documents bucket and are
  # catalogued in g2p_registry_documents. The uploader that does the cataloguing
  # is variant-specific (upload_images.py ships in a variant's db-seed image, not
  # this one) because the images belong to that variant's sample records.
  documentsBucketName: 'documents'
  minioSecure: true

  # Security & Authentication
  keymanagerInstallationName: commons-services-keymanager
  keycloakBaseUrl: 'https://keycloak.{{ tpl .Values.global.baseDomain $ }}'
  # Single shared `staff` realm per namespace — all registry releases (and
  # other apps like AWE) authenticate against the same realm; each release
  # gets its OWN client (clientId below) inside that shared realm.
  keycloakRealm: 'staff'
  keycloakIssuerUrl: '{{ tpl .Values.global.keycloakBaseUrl $ }}/realms/{{ tpl .Values.global.keycloakRealm $ }}'

  # ── Agents (VC issuance) ────────────────────────────────────────────────
  # Agents are NOT staff. They live in their own realm with their own client,
  # so a staff token carries no rights on the Agent Portal API and vice versa.
  # Only read when agentPortalApi.enabled is true.
  # Public host of the agent portal. Deliberately '<release>-agent.<ns>' rather
  # than the 'agent-<registryHostname>' prefix the internal APIs use: this is the
  # host a field agent types, and it must also be a registered redirect URI on
  # the Keycloak agent clients below.
  agentPortalHostname: '{{ .Release.Name }}-agent.{{ tpl .Values.global.baseDomain $ }}'
  # Public URL of iam-agent-portal-api. It is the OIDC confidential client for
  # the agent realm: the agent UI's BFF sends the browser here to start
  # authentication, and Keycloak returns here to have the session established.
  # Distinct from iamPublicUrl, which serves the STAFF realm's provider.
  agentIamPublicUrl: 'https://agent-iam.{{ tpl .Values.global.baseDomain $ }}'
  agentKeycloakRealm: 'agent'
  # Confidential client agents log in through, and whose ROLES carry the
  # issuance permission. Must match the IAM chart's IAM_AGENT_KEYCLOAK_CLIENT_ID
  # and its agent login provider, or an agent authorised at login would be
  # unauthorised here.
  # The agent realm's confidential client. NOT release-prefixed: it is shared,
  # created by commons-services alongside the realm (like staff-portal), and
  # iam-agent-portal-api looks up its secret by exactly this name. The agent's
  # token is minted for it, so resource_access carries the issuance role here.
  agentKeycloakClientId: 'agent-portal'
  # Where the agent portal resolves ROLES -> PERMISSIONS, via
  # /user-access/get_permissions_for_roles.
  #
  # This is the IAM *staff* portal API, not iam-agent-portal-api, which is
  # easy to get wrong: agents LOG IN against the agent API, but only the staff
  # API exposes the user-access endpoints — roles and permissions are held
  # once, for all audiences. Pointing this at the agent API 404s, and the
  # middleware turns that into a blanket 403 on every issuance call.
  #
  # Empty means "use global.iamServiceUrl"; set it only if user-access is
  # served from somewhere else.
  agentAuthProviderApiUrl: ''

  # ── Inji Certify (the credential issuer) ────────────────────────────────
  # One shared Certify per environment, installed with the commons layer. It is
  # reached in-cluster only. Empty audience falls back to the base URL.
  # Where we CALL Certify: its in-cluster Service. Certify ships as part of
  # commons-services (subchart alias openg2p-inji-certify), whose release name is
  # pinned to 'commons-services', so the Service is
  # commons-services-inji-certify. Override if Certify is installed standalone,
  # and use the FQDN (<service>.<namespace>.svc.cluster.local) across namespaces.
  certifyBaseUrl: 'http://commons-services-inji-certify/v1/certify'
  # What Certify expects INSIDE the proof JWT — a different thing from the base
  # URL above. It must appear in Certify's mosip.certify.authn.allowed-audiences,
  # which Certify derives from its PUBLIC domain, not its Service name. Keep this
  # in step with global.certifyHostname on the commons side.
  # Public hostname of Certify. Keep it equal to the commons side's
  # global.certifyHostname — it is what Certify derives its accepted audience
  # from, and (as did:web:<hostname>) its issuer DID.
  certifyHostname: 'certify.{{ tpl .Values.global.baseDomain $ }}'
  # Audience of the OpenID4VCI proof JWT. This is Certify's ISSUER IDENTIFIER --
  # the bare origin, NOT the credential endpoint. Certify validates it with
  # nimbus and rejects anything else:
  #   JWT aud claim has value [https://<host>/v1/certify/issuance/credential],
  #   must be [https://<host>]
  # which surfaces to the agent only as "invalid_proof ... proof jwt parsing",
  # after the beneficiary has already authenticated.
  #
  # Derived from the hostname rather than spelled out, so a deployment on another
  # domain changes ONE value instead of editing a URL with the domain buried in
  # it. Override directly only if Certify's allowed-audiences was customised.
  certifyAudience: 'https://{{ tpl .Values.global.certifyHostname $ }}'

  # The issuer every credential is signed under. ENVIRONMENT-level: one issuing
  # authority per environment, whose keypair Certify's keymanager generates on
  # first boot. `did` MUST equal the DID set at Certify install
  # (commons-services global.vcIssuerDid) — modules reference this issuer, they
  # do not define one. Key alias/algo match Certify's key-alias mapper.
  vcIssuer:
    # Defaults to the SAME derivation the Certify chart uses for its own issuer
    # (commons global.vcIssuerDid = did:web:<certifyHostname>), so a default
    # install agrees with the issuer that will actually sign. Set explicitly only
    # if Certify was installed with a different DID.
    #
    # Deliberately not left empty: issuance is on by default in a manifestation
    # that ships a vcDefinition, and the register Job below refuses to run
    # without a DID -- an empty default failed the whole install.
    did: 'did:web:{{ tpl .Values.global.certifyHostname $ }}'
    keyManagerAppId: CERTIFY_VC_SIGN_ED25519
    keyManagerRefId: ED25519_SIGN
    signatureAlgo: EdDSA
    signatureCryptoSuite: Ed25519Signature2020

  registryKeymanagerAuthEnabled: false
  # Per-release keymanager app id (uppercased Release.Name) so signature
  # scopes don't collide across registry instances in the same environment.
  registryKeymanagerAppId: '{{ .Release.Name | upper }}'

  # ------------------------------------------------------------------
  # Partner signature verification + Consent Manager (PDP) integration
  # ------------------------------------------------------------------
  # The partner-api (a PEP) verifies the DCI envelope signature against partner
  # public keys served by Partner Management (crypto backend "partner-mgmt"),
  # and enforces consent by calling the Consent Manager /validate before
  # returning data — clamping the response to the effective data scopes.
  #
  # Two INDEPENDENT kill-switches over real PII egress, defaulted ON to FAIL
  # CLOSED — matching the registry platform's own defaults.
  #
  #   partnerSignatureValidationEnabled -> verify the DCI envelope signature
  #                                        against the partner's key in Partner
  #                                        Management.
  #   consentEnforcementEnabled         -> call the Consent Manager /validate for
  #                                        the request's consent object, and clamp
  #                                        returned fields to the consented scopes.
  #
  # Turning either OFF is a deliberate, auditable choice, not a default:
  #   * with signature validation off, the `signature` field is still required
  #     but NEVER inspected — any string passes;
  #   * with consent enforcement off, the Consent Manager is never called and
  #     records are returned UNCLAMPED — every field the DCI template emits, to
  #     any caller.
  # Either bypass is logged and stamped into the DCI response header meta
  # (signature_validation / consent_enforcement), which is the only outward
  # signal that it is off.
  #
  # Legitimate reasons to disable: performance testing, or a bring-up install
  # where commons-services (CM/PM) is not yet wired — the DCI search will reject
  # requests until it is. Note the sanity e2e cannot meaningfully test data
  # sharing with these off (it skips those assertions), but the e2e is itself
  # off by default (sanity.runE2e), so a disabled install is unaffected.
  partnerSignatureValidationEnabled: true
  consentEnforcementEnabled: true
  # Partner-key crypto backend: partner-mgmt | keymanager | local
  registryCryptoBackend: partner-mgmt
  # Partner Management partner-api — source of partner public keys. PM is now
  # part of commons-services (service name commons-services-pm-*), matching the
  # g2p-bridge chart. Port 80 is implicit (bare hostname).
  partnerManagementApiUrl: 'http://commons-services-pm-partner-api'
  # Consent Manager partner-api base URL — the /validate PDP endpoint. CM is now
  # part of commons-services (service name commons-services-cm-*), matching PM.
  consentManagerUrl: 'http://commons-services-cm-partner-api'
  consentManagerTimeoutSeconds: 5
  # Used only by the sanity e2e (to seed the shared test partner):
  #  * PM staff-portal-api (onboard + approve the partner key)
  #  * CM staff-portal-api (create the partner binding + policy)
  #  * the CM Keycloak client that holds the CONSENT_MANAGER_ADMIN role
  partnerManagementAdminApiUrl: 'http://commons-services-pm-staff-portal-api'
  consentManagerStaffUrl: 'http://commons-services-cm-api'
  consentManagerAuthClientId: 'consent-manager'
  # Keycloak client the sanity seed authenticates AS to call PM's admin API — it
  # holds PM's `partner_manager` role. PM provisions it as `<pm-release>-staff-portal`
  # with its secret in a Secret of the same name (key client_secret). In the
  # commons install that is `commons-services-staff-portal` (matches g2p-bridge).
  pmSeedClientId: 'commons-services-staff-portal'
  # OIDC client ID — per-release; MUST equal the clientId in keycloak-init.realms below.
  authClientId: '{{ .Release.Name }}-staff-portal'
  # K8s Secret name holding the client password (keycloak-init names the
  # Secret after the clientId, so this must match authClientId above).
  authClientSecret: '{{ .Release.Name }}-staff-portal'
  # Key inside the secret containing the client password
  authClientSecretKey: 'client_secret'

  # IAM service internal URL
  iamServiceUrl: 'http://commons-services-iam-staff-portal-api'

  # Redis-backed auth (refresh tokens + OAuth transaction state) for staff-portal-api.
  # Uses the same shared Redis as iam-staff-portal-api (commons-redis-master).
  authRedisInstallationName: 'commons-redis'
  authRefreshTokenTtlSeconds: 1800
  # Double-submit CSRF validation on state-changing staff-portal-api requests.
  csrfEnabled: true

  # Master Data API internal URL
  masterDataApiUrl: 'http://commons-services-master-data-api'

  # ID Generator
  # Public hostname (used by browser/external callers via Istio VirtualService).
  idGeneratorHostname: 'idgenerator-{{ .Release.Name }}.{{ tpl .Values.global.baseDomain $ }}'
  idGeneratorApiUrl: 'https://{{ tpl .Values.global.idGeneratorHostname $ }}/v1'
  # Internal cluster URL — used by sibling pods (e.g. celery-worker) to reach
  # the ID Generator without hopping through the ingress gateway. The K8s
  # Service is named "<release>-id-generator" on port 80.
  idGeneratorInternalUrl: 'http://{{ .Release.Name }}-id-generator/v1'

  # OpenG2P Audit Manager integration
  # Both `auditEnabled=true` AND `auditManagerUrl` must be set for emission
  # to actually happen — and both are set below, so audit is ON by default.
  # Audit Manager itself is NOT deployed by this chart: it is installed once
  # per environment by commons-services, and the URL below points at it. If it
  # is absent, emission fails silently (the middleware is fire-and-forget) —
  # it does not break requests.
  # Wired into both staff-portal-api and partner-api. Each service consumes
  # these globals via its own envVars block below. Older service images that
  # lack AuditMiddleware simply ignore the env vars — no error.
  auditEnabled: true
  auditManagerUrl: 'http://commons-services-auditmanager:80'
  # Set to false to skip auditing of rejected anonymous calls (401/403
  # without a populated principal). Default true captures attempted
  # unauthorized access for security review.
  auditAnonymousFailures: true

  # ---------------------------------------------------------------------------
  # AWE (Approval Workflow Engine) — NO LONGER installed by this chart. AWE is
  # deployed ONCE per environment by commons-services. This registry CONSUMES
  # that shared instance: it calls AWE's API at aweBaseUrl and registers its own
  # per-registry callback secret into the shared AWE database (via db-seed + the
  # awe-callback-hmac-secret template).
  #
  # The DB globals below must match the fixed contract commons-services' AWE
  # exposes (global.aweDB / aweDBUser / aweDBSecret / aweDBUserPasswordKey) so
  # db-seed can insert this registry's callback_secret row into the shared AWE
  # DB. The DB-user Secret (`awe-db-user`) is created by commons-services in the
  # same namespace, so this registry can read it.
  # ---------------------------------------------------------------------------
  # Shared AWE hostname (matches commons-services' awe.<baseDomain>).
  aweHostname: 'awe.{{ tpl .Values.global.baseDomain $ }}'
  aweDB: awe
  aweDBPort: 5432
  aweDBUser: awe_user
  aweDBSecret: awe-db-user
  aweDBUserPasswordKey: awe-db-user-password

  # Staff-portal-api AWE client + db-seed callback-secret registration
  aweEnabled: true
  aweBaseUrl: 'https://{{ tpl .Values.global.aweHostname $ }}'
  # Callback-secret id — per-registry so the id the staff-portal-api presents
  # (REGISTRY_STAFF_PORTAL_API_AWE_CALLBACK_SECRET_ID) and the row db-seed
  # inserts into the shared AWE `callback_secret` table are unique to THIS
  # registry instance. The extension SQL (awe_meta_data/40_callback_secret.sql.tpl)
  # reads this via the AWE_CALLBACK_SECRET_ID env passed by the db-seed Job.
  aweCallbackSecretId: '{{ .Release.Name }}'
  # Full staff-portal webhook URL stored in AWE callback_secret.caller_service
  # and passed to the registry as REGISTRY_STAFF_PORTAL_API_AWE_DEFAULT_CALLBACK_URL.
  # e.g. https://staff-ofr.nsr.openg2p.org/awe/webhooks/decision
  aweDefaultCallbackUrl: 'https://staff-{{ tpl .Values.global.registryHostname $ }}/awe/webhooks/decision'
  aweHttpTimeoutSeconds: 30
  aweWebhookTimestampToleranceSeconds: 300

  # Register export
  exportQueueVisibilityDays: 2
  exportBatchSize: 2000
  exportNoOfTasksToProcess: 5
  exportBeatProducerFrequency: 20
  exportWorkerMaxAttempts: 3
  exportPresignedUrlExpiryHours: 48
  exportFilesPrefix: "register-exports/"
  aweCallbackHmacSecretName: '{{ .Release.Name }}-awe-callback-hmac'
  aweCallbackHmacSecretKey: 'hmac-secret'
  # NOTE: despite the name, this is the initial Keycloak password for the seeded
  # demo staff users below (keycloak-init), not an AWE resource. Kept here.
  aweApproverUserPassword: 'pass'
  # Password for the demo `agent` user in the Keycloak `agent` realm.
  agentUserPassword: 'pass'
  # Helm release that hosts the shared AWE — its Service is <aweReleaseName>-awe.
  # Used by db-seed to wait for the AWE API before seeding the callback secret.
  aweReleaseName: commons-services

# Logging is handled cluster-wide by OpenTelemetry + Grafana Loki (deployed by
# the platform's infra automation). The OTel agent DaemonSet tails every pod's
# logs automatically, so no per-chart logging config is needed here anymore.

# DB Seed — runs configuration and (optionally) sample data SQL scripts
# after all application pods are ready. Uses a Docker image published
# from openg2p-registry-gen2-extensions.
dbSeed:
  enabled: true
  # LEGACY. Loads a five-level country->village hierarchy into the master_data DB
  # from openg2p-data/geo/geo.csv, keyed by slug-paths like
  # "kamuntu/kilima/karamu/uzima/billyu".
  #
  # Off, and it should stay off. Master Data seeds its own geography from a
  # country pack now (geoSeed.countryPack there), keyed by P-code — so turning
  # this on writes a SECOND hierarchy over the first, in a different id space and
  # with a fixed depth of five levels that only describes Kamuntu. Ethiopia has
  # four, ending at woreda.
  #
  # It is no longer needed for sample data either: load_sample_data.py reads the
  # hierarchy and the sample people from Master Data directly, whatever country
  # and however many levels it holds.
  #
  # Kept only for a deployment that still has no Master Data country pack.
  loadGeoData: false
  # Match the register's geo dropdowns to the hierarchy Master Data holds.
  # An extension's UI metadata names its levels and fixes their number; a
  # country whose pack disagrees gets dropdowns that silently return nothing.
  # Off by default so an existing deployment keeps the metadata it shipped.
  syncGeoWidgets: false
  # Load sample/demo data into the registry (disabled by default).
  # Farmer's db-seed image does this through the intake-form API (household
  # first, then farmer with that household, land before crop/livestock/inputs)
  # and AWE-approves as alex.carter then nina.patel.
  loadSampleData: false
  intakeSeed:
    submitUser: alex.carter
    approver1: alex.carter
    approver2: nina.patel
    maxHouseholds: 50
  # Upload sample profile images (500 colored-initials avatars) to MinIO
  # and update g2p_register_farmers.record_image_document_id. Requires
  # loadSampleData=true (individuals must exist first).
  loadImages: false
  # Upload flat Jinja templates from the db-seed image to MinIO after SQL seed
  loadTemplates: true
  image:
    repository: openg2p/openg2p-registry-db-seed
    tag: "1.2.1"
    pullPolicy: Always

  # DB Seed runs as a one-shot Helm post-install/post-upgrade hook Job.
  # It's just psql replaying SQL scripts plus a one-shot python upload of the
  # sample images to MinIO — minimal footprint. Request 128Mi covers the psql
  # + upload baseline; the 512Mi limit is a generous runaway cap for larger
  # seed bundles (no cpu limit — see staffApi rationale).
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 512Mi

# ---------------------------------------------------------------------------
# IAM registration — self-registers this registry into the IAM staff portal.
# Runs as a one-shot post-install/post-upgrade hook Job that pushes this
# registry's tile + roles/permissions catalog (files/iam-registration/payload.json)
# to the IAM staff-portal API. The application_mnemonic is the per-release
# Keycloak client (global.authClientId), application_url is the Staff Portal
# UI host (https://<registryHostname>), and api_url is the staff API host
# (https://<staffApi.hostname>) used by IAM data policies. Multiple registry
# instances coexist.
#
# Auth: obtains an access token via client_credentials using the per-release
# staff-portal client + secret created by keycloak-init (global.authClientId /
# authClientSecret) — the same client other registry services authenticate
# with. The staff realm must be a trusted login provider in IAM (it is, since
# the registry's own staff-portal-api authenticates against it).
#
# NOTE (CORS): IAM's CORS allow-list is static and read once at IAM startup, so
# this registry's origin (https://<registryHostname>) must also be added to
# IAM_STAFF_CORS_ALLOW_ORIGINS on the IAM chart and IAM rolled. See the IAM repo
# iam-staff-portal-api/samples/README.md.
iamRegister:
  enabled: true
  # Human-readable name of this registry in the IAM application tile. The
  # roles/permissions catalog in files/iam-registration/payload.json is
  # registry-agnostic, so this description is the only variant-specific field —
  # and it is a value (not a file) precisely so a wrapper chart can set it: a
  # subchart's .Files cannot be overridden by its parent.
  applicationDescription: "OpenG2P Registry"
  # Optional staff API base URL stored as api_url on the IAM application.
  # IAM uses this (not a global REGISTRY_API_URL) to load register fields for
  # data policies. Defaults to https://<staffApi.hostname>.
  # apiUrl: ""
  image:
    repository: curlimages/curl
    tag: "8.11.1"
    pullPolicy: IfNotPresent
  # Job-level retry budget; the script also retries token + register internally.
  backoffLimit: 5
  # Internal retry attempts (×5s) while the Keycloak client / IAM finish coming up.
  tokenMaxAttempts: 30
  registerMaxAttempts: 10
  # Tiny one-shot hook Job (token fetch + IAM self-register). Request 64Mi
  # covers the baseline; 128Mi limit is a generous cap (no cpu limit — see
  # staffApi rationale).
  resources:
    requests:
      cpu: 25m
      memory: 64Mi
    limits:
      memory: 128Mi

postgres-init:
  enabled: true
  postgresql:
    host: '{{ .Values.global.postgresqlHost }}'
    port: 5432
    existingSecret: commons-postgresql
    existingSecretPostgresPasswordKey: postgres-password
  databases:
    - name: '{{ tpl .Values.global.registryDB $ }}'
      user: '{{ tpl .Values.global.registryDBUser $ }}'
      secret: '{{ tpl .Values.global.registryDBSecret $ }}'
      secretUserPasswordKey: '{{ tpl .Values.global.registryDBUserPasswordKey $ }}'
      extensions:
        - 'pg_trgm'
    # NOTE: the AWE callback-HMAC secret is NOT provisioned here. A second
    # entry reusing this same db name would render a duplicate postgres-init
    # Job (Helm 4 SSA "spec.template field is immutable") and, with the
    # idempotent init-db.sh, overwrite the registry user's password with the
    # HMAC value. It is minted directly by templates/awe-callback-hmac-secret.yaml.

partnerApi:
  enabled: true
  nameOverride: partner-api
  hostname: 'partner-{{ tpl .Values.global.registryHostname $ }}'

  replicaCount: 1
  # HPA enabled: with 3 workers per pod (below), scale is horizontal. CPU-based
  # only — memory-based HPA is disabled (null target) because Python RSS does
  # not shrink after a spike, so a memory target would scale up and never scale
  # back down. Raise minReplicas to 2+ for HA in production; tune targetCPU and
  # maxReplicas against a load test.
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: null
  updateStrategy:
    type: RollingUpdate
  serviceAccount:
    create: false
    name: ""
  image:
    repository: openg2p/openg2p-registry-partner-api
    tag: "1.2.1"
    pullPolicy: Always
  podSecurityContext:
    enabled: false
    fsGroup: 1001
  containerSecurityContext:
    enabled: false
    runAsUser: 1001
    runAsNonRoot: true

  openapiRootPath: /

  service:
    type: ClusterIP
    port: 80
    externalTrafficPolicy: Cluster

  istio:
    enabled: true
    virtualservice:
      enabled: true
      # Hostname inlined (not `tpl .Values.hostname $`) — the component templates
      # rebind `.Values` to staffApi/partnerApi sub-tree via deepCopy,
      # which boxes string values as interface{} and breaks nested `tpl`.
      # `.Values.global.*` is preserved un-boxed in the rebound context.
      host: 'partner-{{ tpl .Values.global.registryHostname $ }}'
      gateway: "internal"
      destination: '{{ .Release.Name }}-partner-api'
      destinationPort: '{{ .Values.partnerApi.service.port }}'
      prefix: '{{ .Values.openapiRootPath }}/'
      rewriteUri: "/"
    gateway:
      enabled: false
      host: 'partner-{{ tpl .Values.global.registryHostname $ }}'
      ingressController:
        istio: ingressgateway
      httpTlsRedirect: false
      httpEnabled: true
      httpsEnabled: false
      tls:
        mode: SIMPLE
        credentialName: ""

  envVars:
    # gunicorn/uvicorn worker count (overrides the image default). Fewer workers
    # = smaller, more predictable per-pod memory; scale horizontally via the HPA
    # above rather than many workers per pod. Bump this per-environment if
    # concurrent blocking-DB throughput needs it — no image rebuild required.
    PARTNER_NO_OF_WORKERS: "3"
    REGISTRY_PARTNER_API_DB_HOSTNAME: '{{ .Values.global.postgresqlHost }}'
    REGISTRY_PARTNER_API_DB_DBNAME: '{{ tpl .Values.global.registryDB $ }}'
    REGISTRY_PARTNER_API_DB_PORT: '{{ .Values.global.registryDBPort }}'
    REGISTRY_PARTNER_API_DB_USERNAME: '{{ tpl .Values.global.registryDBUser $ }}'
    # SQLAlchemy pool per gunicorn worker process (also used for master-data).
    REGISTRY_PARTNER_API_DB_POOL_SIZE: 5
    REGISTRY_PARTNER_API_DB_POOL_MAX_OVERFLOW: 10
    REGISTRY_PARTNER_API_DB_POOL_PRE_PING: true
    REGISTRY_PARTNER_API_DB_POOL_RECYCLE: 1800
    REGISTRY_PARTNER_API_PORT: '{{ .Values.containerPort }}'

    REGISTRY_PARTNER_API_OPENAPI_ROOT_PATH: '{{ .Values.openapiRootPath }}'

    REGISTRY_PARTNER_API_MASTER_DATA_API_URL: '{{ tpl .Values.global.masterDataApiUrl $ }}'
    REGISTRY_PARTNER_API_MASTER_DATA_TOKEN_URL: "{{ tpl .Values.global.keycloakIssuerUrl $ }}/protocol/openid-connect/token"
    REGISTRY_PARTNER_API_MASTER_DATA_CLIENT_ID: '{{ tpl .Values.global.pmSeedClientId $ }}'

    REGISTRY_PARTNER_API_KEYMANAGER_API_BASE_URL: "{{ .Values.global.keymanagerInstallationName }}/v1/keymanager"
    REGISTRY_PARTNER_API_KEYMANAGER_API_TIMEOUT: 10
    REGISTRY_PARTNER_API_KEYMANAGER_API_DOMAIN: "AUTH"
    REGISTRY_PARTNER_API_KEYMANAGER_SSL_VERIFY: False
    REGISTRY_PARTNER_API_KEYMANAGER_AUTH_ENABLED: '{{ .Values.global.registryKeymanagerAuthEnabled }}'
    REGISTRY_PARTNER_API_KEYMANAGER_AUTH_URL: "{{ tpl .Values.global.keycloakIssuerUrl $ }}/protocol/openid-connect/token"
    REGISTRY_PARTNER_API_KEYMANAGER_AUTH_CLIENT_ID: '{{ tpl .Values.global.authClientId $ }}'
    REGISTRY_PARTNER_API_KEYMANAGER_SIGN_APP_ID: '{{ tpl .Values.global.registryKeymanagerAppId $ }}'
    REGISTRY_PARTNER_API_KEYMANAGER_SIGN_REF_ID: ""

    # Partner signature verification (transport) + Consent Manager enforcement.
    # Both default OFF (global.*Enabled) — enable for production PII egress.
    REGISTRY_PARTNER_API_SIGNATURE_VALIDATION_ENABLED: '{{ .Values.global.partnerSignatureValidationEnabled }}'
    REGISTRY_PARTNER_API_CONSENT_ENFORCEMENT_ENABLED: '{{ .Values.global.consentEnforcementEnabled }}'
    REGISTRY_PARTNER_API_CRYPTO_BACKEND: '{{ .Values.global.registryCryptoBackend }}'
    REGISTRY_PARTNER_API_PARTNER_MGMT_API_URL: '{{ tpl .Values.global.partnerManagementApiUrl $ }}'
    REGISTRY_PARTNER_API_PARTNER_MGMT_ADMIN_API_URL: '{{ tpl .Values.global.partnerManagementAdminApiUrl $ }}'
    REGISTRY_PARTNER_API_PARTNER_MGMT_ADMIN_TOKEN_URL: "{{ tpl .Values.global.keycloakIssuerUrl $ }}/protocol/openid-connect/token"
    REGISTRY_PARTNER_API_PARTNER_MGMT_ADMIN_CLIENT_ID: '{{ tpl .Values.global.pmSeedClientId $ }}'
    REGISTRY_PARTNER_API_CONSENT_MANAGER_URL: '{{ tpl .Values.global.consentManagerUrl $ }}'
    REGISTRY_PARTNER_API_CONSENT_MANAGER_TIMEOUT: '{{ .Values.global.consentManagerTimeoutSeconds }}'

    REGISTRY_PARTNER_API_MINIO_ENDPOINT: '{{ tpl .Values.global.minioHost $ }}'
    REGISTRY_PARTNER_API_MINIO_SECURE: '{{ .Values.global.minioSecure }}'
    # Match documentsBucketName so partner-served record-image URLs resolve.
    REGISTRY_PARTNER_API_MINIO_BUCKET_NAME: '{{ tpl .Values.global.documentsBucketName $ }}'
    REGISTRY_PARTNER_API_TEMPLATE_BUCKET_NAME: '{{ tpl .Values.global.templateBucketName $ }}'

    # OpenG2P Audit Manager integration. No-op unless both AUDIT_ENABLED=true
    # and AUDIT_MANAGER_URL are set (controlled via global.auditEnabled and
    # global.auditManagerUrl). Safe to leave wired in older images that lack
    # the AuditMiddleware — env vars are simply ignored there. Partner-api
    # actor identity defaults to anonymous until controllers opt in to set
    # request.state.audit_actor — see the Audit Manager docs (Integration
    # with Registry → Audit Middleware — Partner API) for the enrichment
    # pattern.
    REGISTRY_PARTNER_API_AUDIT_ENABLED: '{{ .Values.global.auditEnabled }}'
    REGISTRY_PARTNER_API_AUDIT_MANAGER_URL: '{{ .Values.global.auditManagerUrl }}'
    REGISTRY_PARTNER_API_AUDIT_ANONYMOUS_FAILURES: '{{ .Values.global.auditAnonymousFailures }}'
  envVarsFrom:
    REGISTRY_PARTNER_API_DB_PASSWORD:
      secretKeyRef:
        name: '{{ tpl .Values.global.registryDBSecret $ }}'
        key: '{{ tpl .Values.global.registryDBUserPasswordKey $ }}'
    REGISTRY_PARTNER_API_MINIO_ACCESS_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioAccessKey $ }}'
    REGISTRY_PARTNER_API_MINIO_SECRET_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioSecretKey $ }}'
    REGISTRY_PARTNER_API_MINIO_READ_ACCESS_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioReadonlyAccessKey $ }}'
    REGISTRY_PARTNER_API_MINIO_READ_SECRET_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioReadonlySecretKey $ }}'
    REGISTRY_PARTNER_API_OAUTH_CLIENT_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.authClientSecret $ }}'
        key: '{{ tpl .Values.global.authClientSecretKey $ }}'
    REGISTRY_PARTNER_API_KEYMANAGER_AUTH_CLIENT_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.authClientSecret $ }}'
        key: '{{ tpl .Values.global.authClientSecretKey $ }}'
    REGISTRY_PARTNER_API_PARTNER_MGMT_ADMIN_CLIENT_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.pmSeedClientId $ }}'
        key: client_secret
        optional: true
    REGISTRY_PARTNER_API_MASTER_DATA_CLIENT_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.pmSeedClientId $ }}'
        key: client_secret
        optional: true
  startupProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
    successThreshold: 1
  livenessProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 20
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1
  readinessProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1

  postgresCheckerInit:
    enabled: true
    image:
      repository: jbergknoff/postgresql-client
      tag: latest
      pullPolicy: IfNotPresent
    command:
    - 'sh'
    args:
    - '-c'
    - 'until pg_isready -U ${REGISTRY_PARTNER_API_DB_USERNAME} -h ${REGISTRY_PARTNER_API_DB_HOSTNAME} -p ${REGISTRY_PARTNER_API_DB_PORT:-5432} -d ${REGISTRY_PARTNER_API_DB_DBNAME:-registrydb}; do sleep 3; done'
    containerSecurityContext:
      enabled: false

  containerPort: 8000

  # Partner API is a FastAPI process handling partner ingestion calls. Worker
  # count is now 3 (PARTNER_NO_OF_WORKERS in envVars below), reduced from 8 to
  # make memory predictable and shift scale to HPA (enabled above). Sizing
  # mirrored from NSR (same platform/worker profile, measured there): ~500Mi
  # estimated at 3 workers, with spikes during bulk ingest. Request 640Mi /
  # 1.25Gi limit (no cpu limit — see staffApi rationale). ESTIMATE for 3
  # workers — verify with `kubectl top pod` after the first rollout.
  resources:
    requests:
      cpu: 50m
      memory: 640Mi
    limits:
      memory: 1280Mi

beneApi:
  enabled: false
  nameOverride: bene-portal-api
  hostname: 'bene-{{ tpl .Values.global.registryHostname $ }}'
  replicaCount: 1
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
  updateStrategy:
    type: RollingUpdate
  serviceAccount:
    create: false
    name: ""
  image:
    repository: openg2p/openg2p-registry-bene-api
    tag: "1.2.1"
    pullPolicy: Always
  podSecurityContext:
    enabled: false
    fsGroup: 1001
  containerSecurityContext:
    enabled: false
    runAsUser: 1001
    runAsNonRoot: true

  openapiRootPath: /

  service:
    type: ClusterIP
    port: 80
    externalTrafficPolicy: Cluster

  istio:
    enabled: true
    virtualservice:
      enabled: true
      host: ""
      gateway: "internal"
      destination: '{{ .Release.Name }}-bene-portal-api'
      destinationPort: '{{ .Values.beneApi.service.port }}'
      prefix: '{{ .Values.openapiRootPath }}/'
      rewriteUri: "/"
    gateway:
      enabled: false
      host: ""
      ingressController:
        istio: ingressgateway
      httpTlsRedirect: false
      httpEnabled: true
      httpsEnabled: false
      tls:
        mode: SIMPLE
        credentialName: ""

  envVars:
    REGISTRY_BENE_PORTAL_API_DB_HOSTNAME: '{{ .Values.global.postgresqlHost }}'
    REGISTRY_BENE_PORTAL_API_DB_DBNAME: '{{ tpl .Values.global.registryDB $ }}'
    REGISTRY_BENE_PORTAL_API_DB_PORT: '{{ .Values.global.registryDBPort }}'
    REGISTRY_BENE_PORTAL_API_DB_USERNAME: '{{ tpl .Values.global.registryDBUser $ }}'
    # SQLAlchemy pool per gunicorn worker process.
    REGISTRY_BENE_PORTAL_API_DB_POOL_SIZE: 5
    REGISTRY_BENE_PORTAL_API_DB_POOL_MAX_OVERFLOW: 10
    REGISTRY_BENE_PORTAL_API_DB_POOL_PRE_PING: true
    REGISTRY_BENE_PORTAL_API_DB_POOL_RECYCLE: 1800

    REGISTRY_BENE_PORTAL_API_OPENAPI_ROOT_PATH: '{{ .Values.openapiRootPath }}'

    REGISTRY_BENE_PORTAL_API_MINIO_ENDPOINT: '{{ tpl .Values.global.minioHost $ }}'
    REGISTRY_BENE_PORTAL_API_MINIO_SECURE: '{{ .Values.global.minioSecure }}'
    # DocumentBucket.TEMPLATES hard-sets the physical bucket to "templates".
    REGISTRY_BENE_PORTAL_API_MINIO_BUCKET_NAME: "templates"
  envVarsFrom:
    REGISTRY_BENE_PORTAL_API_DB_PASSWORD:
      secretKeyRef:
        name: '{{ tpl .Values.global.registryDBSecret $ }}'
        key: '{{ tpl .Values.global.registryDBUserPasswordKey $ }}'
    REGISTRY_BENE_PORTAL_API_MINIO_ACCESS_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioAccessKey $ }}'
    REGISTRY_BENE_PORTAL_API_MINIO_SECRET_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioSecretKey $ }}'
    REGISTRY_BENE_PORTAL_API_MINIO_READ_ACCESS_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioReadonlyAccessKey $ }}'
    REGISTRY_BENE_PORTAL_API_MINIO_READ_SECRET_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioReadonlySecretKey $ }}'

  startupProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
    successThreshold: 1
  livenessProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 20
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1
  readinessProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1

  postgresCheckerInit:
    enabled: true
    image:
      repository: jbergknoff/postgresql-client
      tag: latest
      pullPolicy: IfNotPresent
    command:
    - 'sh'
    args:
    - '-c'
    - 'until pg_isready -U ${REGISTRY_BENE_PORTAL_API_DB_USERNAME} -h ${REGISTRY_BENE_PORTAL_API_DB_HOSTNAME} -p ${REGISTRY_BENE_PORTAL_API_DB_PORT:-5432} -d ${REGISTRY_BENE_PORTAL_API_DB_DBNAME:-registrydb}; do sleep 3; done'
    containerSecurityContext:
      enabled: false

  containerPort: 8000

  # Beneficiary Portal API is a FastAPI process (same profile as Partner API)
  # serving the beneficiary self-service portal. Disabled by default (enabled:
  # false above); no live measurement, so it mirrors Partner API's 3-worker
  # envelope (640Mi request / 1.25Gi limit). No cpu limit (see staffApi).
  resources:
    requests:
      cpu: 50m
      memory: 640Mi
    limits:
      memory: 1280Mi

# ==========================================================================
# Agent Portal API — verifiable credential issuance at an agent counter.
#
# OFF BY DEFAULT, twice over:
#   agentPortalApi.enabled            — deploy the service at all
#   agentPortalApi.vcIssuance.enabled — mount the issuance routes inside it
# With both false the chart renders exactly what it did before this component
# existed, so an existing registry can take this chart version with no change
# in behaviour.
#
# Agents are a DISTINCT audience from staff: their own Keycloak realm
# (`agent`), their own portal, their own permission. A staff token grants
# nothing here.
#
# Turning it on additionally needs, from the MANIFESTATION (NSR, Farmer
# Registry, …) because the claim fields differ per registry:
#   * a read-only VC view keyed on internal_record_id (see vcDefinitions[].view)
#   * its vcDefinitions entries
#   * the SVG card design (svgConfigMap)
# and, from the environment: the `agent` realm, and an eSignet provider row for
# registrant authentication.
# ==========================================================================
agentPortalApi:
  enabled: false
  nameOverride: agent-portal-api
  hostname: 'agent-{{ tpl .Values.global.registryHostname $ }}'
  replicaCount: 1

  # The capability switch inside the service. Kept separate from `enabled` so a
  # deployment can run the agent portal without credential issuance.
  # ── Credential VERIFICATION (agent checks a card someone presents) ───────
  # Independent of issuance on purpose. Needs openg2p-inji-verify deployed
  # (verifiable-credentials repo) — that chart runs MOSIP's verify-service
  # stateless, with no config-server and no database.
  vcVerification:
    enabled: false
    # Where verify-service listens. Default matches the openg2p-inji-verify
    # chart installed as a subchart of commons-services.
    serviceUrl: 'http://commons-services-inji-verify-service/v1/verify'

  vcIssuance:
    enabled: false
    # How long a completed beneficiary authentication may authorise an issuance,
    # measured from its completion. Deliberately short: the beneficiary is
    # standing at the counter. This is VC policy and is independent of the
    # (much longer) validity the authentication record carries for other users
    # of the same subsystem.
    authWindowSeconds: 300
    # Register whose records may be issued credentials. Phase 1 issues to an
    # individual; set this to that register's id.
    registerId: ""
    # Registrant-authentication provider to use (adapter_name = esignet). Empty
    # means "the first active provider on the register".
    # Created by the bootstrap Job below, and read by the API at issuance —
    # both derive it from here, so they cannot drift.
    authProviderId: 'esignet-{{ .Release.Name }}'
    # Register this registry's credential_configs with Certify on install and
    # upgrade. Certify can only issue a type it already knows, so leaving this
    # off means every issuance fails on an unknown credential_configuration_id
    # — turn it off only when the configs are managed by hand.
    registerEnabled: true
    registerImage: curlimages/curl:8.11.0

    # ── One-time bootstrap of the beneficiary-authentication chain ───────────
    # Creates the four pieces of state that VC issuance needs and that nothing
    # else creates: the eSignet OIDC client (with a freshly generated keypair),
    # this register's eSignet provider row, the IAM application/permission/role
    # that authorises issuance, and the register's
    # requires_registrant_authentication flag.
    #
    # It writes into eSignet's and IAM's schemas as well as the registry's.
    # That is why it is a switch. eSignet's client-mgmt API is gated behind an
    # OAuth scope (add_oidc_client) an unattended job cannot obtain, so seeding
    # the row is the only route; turn this off where another system owns that
    # state and do the four steps yourself.
    #
    # Idempotent, and it never rotates a live key: if the provider row already
    # carries a private key the client and provider are left untouched, because
    # eSignet holds the matching public half and replacing one side silently
    # breaks every authentication.
    bootstrap:
      enabled: true
      backoffLimit: 3
      # Key generation needs openssl, which the postgres client image does not
      # carry — hence a separate init container. python:3.12-slim is used
      # because it ships the openssl CLI; alpine/openssl does not resolve on
      # Docker Hub, and Bitnami's images are no longer freely published.
      keygenImage: python:3.12-slim
      psqlImage: postgres:16-alpine
      # Reads and writes the one Secret below; needs nothing else. alpine/k8s is
      # what the rest of this chart set already uses for kubectl steps -- Bitnami
      # no longer publishes these freely, so it is not an option here.
      kubectlImage: alpine/k8s:1.31.0
      # Keep the client keypair in a Secret as well as the registry database.
      #
      # The database alone is not enough. The private key is the credential
      # eSignet authenticates this registry with, and the registry schema is
      # rebuilt by a reinstall -- so the row disappears, the bootstrap Job sees
      # no key, and mints a new one. That ROTATES a live credential as a side
      # effect of reinstalling: eSignet keeps the old public half, and every
      # beneficiary authentication then fails with `invalid_assertion`, which
      # looks like a misconfiguration and gives no clue that a key moved.
      #
      # A Secret outlives the schema, so the key is restored and reused instead.
      # Turn this off only where something else owns the keypair's lifecycle --
      # and then expect to re-register the client on eSignet after every reseed.
      persistKeyToSecret:
        enabled: true
        secretName: '{{ .Release.Name }}-registrant-auth-client-key'
      esignet:
        # Public host, used to derive the authorize/token/userinfo endpoints and
        # the issuer the provider row trusts.
        hostname: 'esignet.{{ tpl .Values.global.baseDomain $ }}'
        # Per-release so two registries in one namespace get their own eSignet
        # client and their own keypair.
        clientId: '{{ .Release.Name }}-registry-vc'
        # Authentication factor eSignet should ask the beneficiary for.
        # mosip:idp:acr:generated-code = OTP, :biometrics = fingerprint/iris.
        acrValues: 'mosip:idp:acr:generated-code'
        dbName: mosip_esignet
        dbUser: esignetuser
        dbSecret: esignet-db-user
        dbSecretKey: esignet-db-user-password
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          memory: 256Mi

  # One entry per issuable credential type. The base platform ships NONE: the
  # fields differ per registry, so the concrete list belongs to the
  # manifestation's chart. Shape:
  #
  #   - config_id: OpenG2PBeneficiaryCredential      # Certify credential_config id
  #     credential_types: ["VerifiableCredential", "OpenG2PBeneficiaryCredential"]
  #     view: beneficiary_vc_view                    # keyed on internal_record_id
  #     claim_columns: ["functionalRecordId", "fullName", "dateOfBirth"]
  #     svg_template: beneficiary-card.svg
  #     display_name: "Beneficiary Credential"
  #     # Registered into Certify by the credential-config-register Job. The
  #     # issuer (DID + key) is added from global.vcIssuer — do not repeat it.
  #     certifyConfig:
  #       credentialConfigKeyId: OpenG2PBeneficiaryCredential
  #       credentialFormat: ldp_vc
  #       credentialTypes: ["VerifiableCredential", "OpenG2PBeneficiaryCredential"]
  #       contextURLs: ["https://www.w3.org/2018/credentials/v1"]
  #       scope: openg2p_beneficiary_vc_ldp
  #       displayOrder: ["functionalRecordId", "fullName", "dateOfBirth"]
  #       metaDataDisplay: [{ name: "OpenG2P Beneficiary Credential", locale: en }]
  #       credentialSubjectDefinition: { ... }
  #       # Authored as READABLE JSON; the Job base64-encodes it for Certify.
  #       vcTemplateJson: |
  #         { "@context": [...], "issuer": "${_issuer}", ... }
  vcDefinitions: []

  # ConfigMap holding the SVG card designs, mounted at /app/pdf-templates. The
  # manifestation builds it from real .svg files kept in its own repo, so a
  # designer can change the card without touching code or this chart.
  svgConfigMap: ""

  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
  updateStrategy:
    type: RollingUpdate
  serviceAccount:
    create: false
    name: ""
  image:
    repository: openg2p/openg2p-registry-agent-portal-api
    tag: "1.0"
    pullPolicy: Always
  podSecurityContext:
    enabled: false
    fsGroup: 1001
  containerSecurityContext:
    enabled: false
    runAsUser: 1001
    runAsNonRoot: true

  openapiRootPath: /

  service:
    type: ClusterIP
    port: 80
    externalTrafficPolicy: Cluster

  istio:
    enabled: true
    virtualservice:
      enabled: true
      host: ""
      gateway: "internal"
      destination: '{{ .Release.Name }}-agent-portal-api'
      destinationPort: '{{ .Values.agentPortalApi.service.port }}'
      prefix: '{{ .Values.openapiRootPath }}/'
      rewriteUri: "/"
    gateway:
      enabled: false
      host: ""
      ingressController:
        istio: ingressgateway
      httpTlsRedirect: false
      httpEnabled: true
      httpsEnabled: false
      tls:
        mode: SIMPLE
        credentialName: ""

  envVars:
    # Must match IAM_AGENT_AUTH_COOKIE_PREFIX on iam-agent-portal-api: that
    # service SETS the auth cookies and this one READS them. The agent and
    # staff portals share a parent cookie domain, so unprefixed names collide
    # and this API is handed the staff realm's ID token alongside a valid agent
    # bearer token -- a token it cannot verify against the agent realm's JWKS.
    REGISTRY_AGENT_PORTAL_API_AUTH_COOKIE_PREFIX: 'agent-'
    # iam_core's ValidateAndRefreshTokenMiddleware looks the browser session's
    # refresh token up in Redis, so this MUST be the same instance the IAM
    # services write to (authRedisInstallationName), not this registry's own
    # Redis -- a different instance means the session is never found and every
    # authenticated call fails. Defaults to localhost when unset, which is a
    # connection-refused 500 on the first request, not a clear error.
    REGISTRY_AGENT_PORTAL_API_AUTH_REDIS_URL: 'redis://{{ tpl .Values.global.authRedisInstallationName $ }}-master:6379/0'

    REGISTRY_AGENT_PORTAL_API_DB_HOSTNAME: '{{ .Values.global.postgresqlHost }}'
    REGISTRY_AGENT_PORTAL_API_DB_DBNAME: '{{ tpl .Values.global.registryDB $ }}'
    REGISTRY_AGENT_PORTAL_API_DB_PORT: '{{ .Values.global.registryDBPort }}'
    REGISTRY_AGENT_PORTAL_API_DB_USERNAME: '{{ tpl .Values.global.registryDBUser $ }}'

    REGISTRY_AGENT_PORTAL_API_OPENAPI_ROOT_PATH: '{{ .Values.openapiRootPath }}'

    # Feature switch, read by the service at startup.
    REGISTRY_AGENT_PORTAL_API_VC_ISSUANCE_ENABLED: '{{ .Values.vcIssuance.enabled }}'
    # Verification is switched separately from issuance: a deployment may want
    # agents to CHECK a card without being able to create one.
    REGISTRY_AGENT_PORTAL_API_VC_VERIFICATION_ENABLED: '{{ .Values.vcVerification.enabled }}'
    # Inji Verify's verify-service, in-cluster. The browser never reaches it:
    # the UI posts to this API, which calls it here — the same shape as every
    # other backend call the portal makes.
    #
    # It resolves the ISSUER'S key itself, from Certify's
    # /v1/certify/.well-known/jwks.json -- reachable inside the cluster, so no
    # key is copied into the verifier or kept in step by hand.
    #
    # JWKS specifically, NOT the did:web document. Certify builds the DID
    # document's verificationMethod list from the REGISTERED CREDENTIAL CONFIGS'
    # signature algorithm (DIDDocumentUtil.getSignatureCryptoSuiteMap reads
    # credential_config.signature_algo), so it carries the EdDSA key that signs
    # the JSON-LD credential and NOT the ES256 key that signs the claim-169 QR --
    # qrSignatureAlgo is not part of that map. JwksServiceImpl instead walks
    # mosip.certify.signature-algo.key-alias-mapper and publishes every alias,
    # so the QR's ES256 key is only in the JWKS. On paper the QR signature is
    # the one anyone checks, so this distinction decides whether verification
    # can work at all.
    REGISTRY_AGENT_PORTAL_API_VERIFY_SERVICE_URL: '{{ tpl .Values.vcVerification.serviceUrl $ }}'
    REGISTRY_AGENT_PORTAL_API_VC_AUTH_WINDOW_SECONDS: '{{ .Values.vcIssuance.authWindowSeconds }}'
    REGISTRY_AGENT_PORTAL_API_VC_REGISTER_ID: '{{ .Values.vcIssuance.registerId }}'
    REGISTRY_AGENT_PORTAL_API_VC_AUTH_PROVIDER_ID: '{{ tpl .Values.vcIssuance.authProviderId $ }}'
    REGISTRY_AGENT_PORTAL_API_VC_DEFINITIONS: '{{ .Values.vcDefinitions | toJson }}'

    # Inji Certify, reached in-cluster. Certify is never exposed publicly for
    # issuance; this service is its only client.
    # tpl'd explicitly: each env value gets exactly ONE tpl pass, and plain
    # indirection returns the global's raw text without re-evaluating it — so a
    # templated global (the audience carries .Release.Namespace) would reach the
    # pod as literal template source.
    REGISTRY_AGENT_PORTAL_API_CERTIFY_BASE_URL: '{{ tpl .Values.global.certifyBaseUrl $ }}'
    REGISTRY_AGENT_PORTAL_API_CERTIFY_AUDIENCE: '{{ tpl .Values.global.certifyAudience $ }}'

    REGISTRY_AGENT_PORTAL_API_SVG_TEMPLATE_DIR: /app/pdf-templates
    REGISTRY_AGENT_PORTAL_API_PDF_TITLE: '{{ .Values.pdf.title }}'
    REGISTRY_AGENT_PORTAL_API_PDF_ISSUER_NAME: '{{ .Values.pdf.issuerName }}'

    # IAM — the `agent` realm client this API validates tokens against.
    REGISTRY_AGENT_PORTAL_API_KEYCLOAK_CLIENT_ID: '{{ tpl .Values.global.agentKeycloakClientId $ }}'
    # Audit Manager. Issuance is the most consequential action here, so this is
    # on by default; it still no-ops if the URL is empty.
    REGISTRY_AGENT_PORTAL_API_AUDIT_ENABLED: '{{ .Values.global.auditEnabled }}'
    REGISTRY_AGENT_PORTAL_API_AUDIT_MANAGER_URL: '{{ tpl .Values.global.auditManagerUrl $ }}'
    REGISTRY_AGENT_PORTAL_API_AUDIT_ANONYMOUS_FAILURES: '{{ .Values.global.auditAnonymousFailures }}'
    # ── Registrant-authentication session store ───────────────────────────
    # The eSignet transaction (state/nonce/PKCE verifier + the record context)
    # is created when authentication STARTS and read back when eSignet
    # redirects to the callback. Those two halves do not run in the same pod:
    # the agent portal starts it, but oauth_callback_url points at the STAFF
    # API, which is where eSignet lands. With the default in-process store the
    # callback pod has never seen the state and the authentication always
    # fails. It must therefore be a store both pods share.
    #
    # The code's own default is redis://localhost:6379/0, which resolves to
    # nothing inside either pod, so this cannot be left unset.
    REGISTRY_CORE_REGISTRANT_AUTH_SESSION_STORE_BACKEND: 'redis'
    REGISTRY_CORE_REGISTRANT_AUTH_REDIS_URL: 'redis://{{ tpl .Values.global.redisInstallationName $ }}:6379/0'
    REGISTRY_AGENT_PORTAL_API_AUTH_PROVIDER_API_URL: '{{ tpl (default .Values.global.iamServiceUrl .Values.global.agentAuthProviderApiUrl) $ }}'
  envVarsFrom:
    REGISTRY_AGENT_PORTAL_API_DB_PASSWORD:
      secretKeyRef:
        name: '{{ tpl .Values.global.registryDBSecret $ }}'
        key: '{{ tpl .Values.global.registryDBUserPasswordKey $ }}'

  pdf:
    title: "Beneficiary Credential"
    issuerName: "OpenG2P"

  startupProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
    successThreshold: 1
  livenessProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 20
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1
  readinessProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1

  postgresCheckerInit:
    enabled: true
    image:
      repository: jbergknoff/postgresql-client
      tag: latest
      pullPolicy: IfNotPresent
    command:
    - 'sh'
    args:
    - '-c'
    - 'until pg_isready -U ${REGISTRY_AGENT_PORTAL_API_DB_USERNAME} -h ${REGISTRY_AGENT_PORTAL_API_DB_HOSTNAME} -p ${REGISTRY_AGENT_PORTAL_API_DB_PORT:-5432} -d ${REGISTRY_AGENT_PORTAL_API_DB_DBNAME:-registrydb}; do sleep 3; done'
    containerSecurityContext:
      enabled: false

  containerPort: 8000

  # Same FastAPI profile as the Beneficiary Portal API (3 workers). Rendering a
  # PDF is short-lived and CPU-bound; no cpu limit (see staffApi).
  resources:
    requests:
      cpu: 50m
      memory: 640Mi
    limits:
      memory: 1280Mi

staffApi:
  enabled: true
  nameOverride: staff-portal-api
  hostname: 'staff-{{ tpl .Values.global.registryHostname $ }}'
  replicaCount: 1
  # HPA enabled: with 3 workers per pod (below), scale is horizontal. CPU-based
  # only — memory-based HPA is disabled (null target) because Python RSS does
  # not shrink after a spike, so a memory target would scale up and never scale
  # back down. Raise minReplicas to 2+ for HA in production; tune targetCPU and
  # maxReplicas against a load test.
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: null
  updateStrategy:
    type: RollingUpdate
  serviceAccount:
    create: false
    name: ""
  image:
    repository: openg2p/openg2p-registry-staff-api
    tag: "1.2.1"
    pullPolicy: Always
  podSecurityContext:
    enabled: false
    fsGroup: 1001
  containerSecurityContext:
    enabled: false
    runAsUser: 1001
    runAsNonRoot: true

  openapiRootPath: /

  service:
    type: ClusterIP
    port: 80
    externalTrafficPolicy: Cluster

  istio:
    enabled: true
    virtualservice:
      enabled: true
      # Hostname inlined — see comment under partnerApi.istio for rationale.
      host: 'staff-{{ tpl .Values.global.registryHostname $ }}'
      gateway: "internal"
      destination: '{{ .Release.Name }}-staff-portal-api'
      destinationPort: '{{ .Values.staffApi.service.port }}'
      prefix: '{{ .Values.openapiRootPath }}/'
      rewriteUri: "/"
    gateway:
      enabled: false
      host: 'staff-{{ tpl .Values.global.registryHostname $ }}'
      ingressController:
        istio: ingressgateway
      httpTlsRedirect: false
      httpEnabled: true
      httpsEnabled: false
      tls:
        mode: SIMPLE
        credentialName: ""

  envVars:
    # gunicorn/uvicorn worker count (overrides the image default). Fewer workers
    # = smaller, more predictable per-pod memory; scale horizontally via the HPA
    # above rather than many workers per pod. Bump this per-environment if
    # concurrent blocking-DB throughput needs it — no image rebuild required.
    STAFF_PORTAL_NO_OF_WORKERS: "3"
    REGISTRY_STAFF_PORTAL_API_DB_HOSTNAME: '{{ .Values.global.postgresqlHost }}'
    REGISTRY_STAFF_PORTAL_API_DB_DBNAME: '{{ tpl .Values.global.registryDB $ }}'
    REGISTRY_STAFF_PORTAL_API_DB_PORT: '{{ .Values.global.registryDBPort }}'
    REGISTRY_STAFF_PORTAL_API_DB_USERNAME: '{{ tpl .Values.global.registryDBUser $ }}'
    # SQLAlchemy pool per gunicorn worker process (also used for master-data).
    # 5+10 with STAFF_PORTAL_NO_OF_WORKERS=3 stays well under typical Postgres
    # max_connections; raise these if you bump workers without adding pods.
    REGISTRY_STAFF_PORTAL_API_DB_POOL_SIZE: 5
    REGISTRY_STAFF_PORTAL_API_DB_POOL_MAX_OVERFLOW: 10
    REGISTRY_STAFF_PORTAL_API_DB_POOL_PRE_PING: true
    REGISTRY_STAFF_PORTAL_API_DB_POOL_RECYCLE: 1800

    REGISTRY_STAFF_PORTAL_API_MASTER_DATA_API_URL: '{{ tpl .Values.global.masterDataApiUrl $ }}'
    REGISTRY_STAFF_PORTAL_API_MASTER_DATA_TOKEN_URL: "{{ tpl .Values.global.keycloakIssuerUrl $ }}/protocol/openid-connect/token"
    REGISTRY_STAFF_PORTAL_API_MASTER_DATA_CLIENT_ID: '{{ tpl .Values.global.pmSeedClientId $ }}'

    REGISTRY_STAFF_PORTAL_API_OPENAPI_ROOT_PATH: '{{ .Values.openapiRootPath }}'

    REGISTRY_STAFF_PORTAL_API_AUTH_PROVIDER_API_URL: '{{ .Values.global.iamServiceUrl }}'
    REGISTRY_STAFF_PORTAL_API_AUTH_REDIS_URL: 'redis://{{ tpl .Values.global.authRedisInstallationName $ }}-master:6379/0'
    # ── Registrant-authentication session store ───────────────────────────
    # The eSignet transaction (state/nonce/PKCE verifier + the record context)
    # is created when authentication STARTS and read back when eSignet
    # redirects to the callback. Those two halves do not run in the same pod:
    # the agent portal starts it, but oauth_callback_url points at the STAFF
    # API, which is where eSignet lands. With the default in-process store the
    # callback pod has never seen the state and the authentication always
    # fails. It must therefore be a store both pods share.
    #
    # The code's own default is redis://localhost:6379/0, which resolves to
    # nothing inside either pod, so this cannot be left unset.
    REGISTRY_CORE_REGISTRANT_AUTH_SESSION_STORE_BACKEND: 'redis'
    REGISTRY_CORE_REGISTRANT_AUTH_REDIS_URL: 'redis://{{ tpl .Values.global.redisInstallationName $ }}:6379/0'
    REGISTRY_STAFF_PORTAL_API_AUTH_REFRESH_TOKEN_TTL_SECONDS: '{{ .Values.global.authRefreshTokenTtlSeconds }}'
    REGISTRY_STAFF_PORTAL_API_CSRF_ENABLED: '{{ .Values.global.csrfEnabled }}'
    REGISTRY_STAFF_PORTAL_API_KEYCLOAK_CLIENT_ID: '{{ tpl .Values.global.authClientId $ }}'
    REGISTRY_STAFF_PORTAL_API_MINIO_ENDPOINT: '{{ tpl .Values.global.minioHost $ }}'
    REGISTRY_STAFF_PORTAL_API_MINIO_SECURE: '{{ .Values.global.minioSecure }}'
    # Record images (incl. db-seed uploads) live in documentsBucketName; this must
    # match so staff-portal presigned record-image URLs resolve.
    REGISTRY_STAFF_PORTAL_API_MINIO_BUCKET_NAME: '{{ tpl .Values.global.documentsBucketName $ }}'
    REGISTRY_STAFF_PORTAL_API_TEMPLATE_BUCKET_NAME: '{{ tpl .Values.global.templateBucketName $ }}'
    REGISTRY_STAFF_PORTAL_API_EXPORT_QUEUE_VISIBILITY_DAYS: '{{ .Values.global.exportQueueVisibilityDays }}'
    REGISTRY_STAFF_PORTAL_API_EXPORT_BATCH_SIZE: '{{ .Values.global.exportBatchSize }}'

    # OpenG2P Audit Manager integration. No-op unless both AUDIT_ENABLED=true
    # and AUDIT_MANAGER_URL are set (controlled via global.auditEnabled and
    # global.auditManagerUrl). Safe to leave wired in older images that lack
    # the AuditMiddleware — env vars are simply ignored there.
    REGISTRY_STAFF_PORTAL_API_AUDIT_ENABLED: '{{ .Values.global.auditEnabled }}'
    REGISTRY_STAFF_PORTAL_API_AUDIT_MANAGER_URL: '{{ .Values.global.auditManagerUrl }}'
    REGISTRY_STAFF_PORTAL_API_AUDIT_ANONYMOUS_FAILURES: '{{ .Values.global.auditAnonymousFailures }}'

    REGISTRY_STAFF_PORTAL_API_AWE_ENABLED: '{{ .Values.global.aweEnabled }}'
    REGISTRY_STAFF_PORTAL_API_AWE_BASE_URL: '{{ tpl .Values.global.aweBaseUrl $ }}'
    REGISTRY_STAFF_PORTAL_API_AWE_HTTP_TIMEOUT_SECONDS: '{{ .Values.global.aweHttpTimeoutSeconds }}'
    REGISTRY_STAFF_PORTAL_API_AWE_DEFAULT_CALLBACK_URL: '{{ tpl .Values.global.aweDefaultCallbackUrl $ }}'
    REGISTRY_STAFF_PORTAL_API_AWE_CALLBACK_SECRET_ID: '{{ tpl .Values.global.aweCallbackSecretId $ }}'
    REGISTRY_STAFF_PORTAL_API_AWE_WEBHOOK_TIMESTAMP_TOLERANCE_SECONDS: '{{ .Values.global.aweWebhookTimestampToleranceSeconds }}'

    REGISTRY_STAFF_PORTAL_API_PARTNER_MGMT_API_URL: '{{ tpl .Values.global.partnerManagementApiUrl $ }}'
    REGISTRY_STAFF_PORTAL_API_PARTNER_MGMT_ADMIN_API_URL: '{{ tpl .Values.global.partnerManagementAdminApiUrl $ }}'
    REGISTRY_STAFF_PORTAL_API_PARTNER_MGMT_ADMIN_TOKEN_URL: "{{ tpl .Values.global.keycloakIssuerUrl $ }}/protocol/openid-connect/token"
    REGISTRY_STAFF_PORTAL_API_PARTNER_MGMT_ADMIN_CLIENT_ID: '{{ tpl .Values.global.pmSeedClientId $ }}'

  envVarsFrom:
    REGISTRY_STAFF_PORTAL_API_DB_PASSWORD:
      secretKeyRef:
        name: '{{ tpl .Values.global.registryDBSecret $ }}'
        key: '{{ tpl .Values.global.registryDBUserPasswordKey $ }}'
    REGISTRY_STAFF_PORTAL_API_MINIO_ACCESS_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioAccessKey $ }}'
    REGISTRY_STAFF_PORTAL_API_MINIO_SECRET_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioSecretKey $ }}'
    REGISTRY_STAFF_PORTAL_API_MINIO_READ_ACCESS_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioReadonlyAccessKey $ }}'
    REGISTRY_STAFF_PORTAL_API_MINIO_READ_SECRET_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioReadonlySecretKey $ }}'
    REGISTRY_STAFF_PORTAL_API_AWE_CALLBACK_HMAC_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.aweCallbackHmacSecretName $ }}'
        key: '{{ .Values.global.aweCallbackHmacSecretKey }}'
    REGISTRY_STAFF_PORTAL_API_PARTNER_MGMT_ADMIN_CLIENT_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.pmSeedClientId $ }}'
        key: client_secret
        optional: true
    REGISTRY_STAFF_PORTAL_API_MASTER_DATA_CLIENT_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.pmSeedClientId $ }}'
        key: client_secret
        optional: true

  startupProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
    successThreshold: 1
  livenessProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 20
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1
  readinessProbe:
    enabled: true
    httpGet:
      path: /ping
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1

  postgresCheckerInit:
    enabled: true
    image:
      repository: jbergknoff/postgresql-client
      tag: latest
      pullPolicy: IfNotPresent
    command:
    - 'sh'
    args:
    - '-c'
    - 'until pg_isready -U ${REGISTRY_STAFF_PORTAL_API_DB_USERNAME} -h ${REGISTRY_STAFF_PORTAL_API_DB_HOSTNAME} -p ${REGISTRY_STAFF_PORTAL_API_DB_PORT:-5432} -d ${REGISTRY_STAFF_PORTAL_API_DB_DBNAME:-registrydb}; do sleep 3; done'
    containerSecurityContext:
      enabled: false

  containerPort: 8000

  # Staff Portal API is the primary read-write FastAPI for registry staff
  # operations (registrant CRUD, lookup, search, intake validation, MinIO
  # pre-signed URLs). Worker count is now 3 (STAFF_PORTAL_NO_OF_WORKERS in
  # envVars below), reduced from 8 to make memory predictable and shift scale
  # to HPA (enabled above). Sizing mirrored from NSR (measured there): ~570Mi
  # estimated at 3 workers; search/listing spikes as result pages materialise
  # as Python objects. Request 768Mi / 1.5Gi limit — a GENEROUS runaway cap,
  # NOT a right-size (no cpu limit: CPU is compressible, a cpu limit only
  # throttles latency and never prevents OOM; memory limits protect the node
  # from a single runaway pod). ESTIMATE for 3 workers — verify with
  # `kubectl top pod` after the first rollout.
  resources:
    requests:
      cpu: 50m
      memory: 768Mi
    limits:
      memory: 1536Mi

staffUi:
  enabled: true
  nameOverride: staff-portal-ui
  hostname: '{{ tpl .Values.global.registryHostname $ }}'
  replicaCount: 1
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
  updateStrategy:
    type: RollingUpdate
  serviceAccount:
    create: false
    name: ""
  image:
    repository: openg2p/openg2p-registry-staff-ui
    tag: "1.2.1"
    pullPolicy: Always
  podSecurityContext:
    enabled: false
    fsGroup: 1001
  containerSecurityContext:
    enabled: false
    runAsUser: 1001
    runAsNonRoot: true

  service:
    type: ClusterIP
    port: 80
    externalTrafficPolicy: Cluster

  containerPort: 3000

  # Staff Portal UI is a Next.js SSR app. Node's V8 heap can balloon on cold
  # start (JIT warmup), large page pre-render, or concurrent SSR. Sizing
  # mirrored from NSR (~67Mi idle measured there): request 192Mi covers the
  # baseline with headroom; 768Mi limit is a generous cap matching typical
  # Next.js production sizing (no cpu limit — see staffApi rationale).
  resources:
    requests:
      cpu: 50m
      memory: 192Mi
    limits:
      memory: 768Mi

  istio:
    enabled: true
    virtualservice:
      enabled: true
      host: '{{ tpl .Values.global.registryHostname $ }}'
      gateway: "internal"
      destination: '{{ .Release.Name }}-staff-portal-ui'
      destinationPort: '{{ .Values.service.port }}'
      prefix: '/'
      rewriteUri: "/"
    gateway:
      enabled: false
      host: '{{ tpl .Values.global.registryHostname $ }}'
      ingressController:
        istio: ingressgateway
      httpTlsRedirect: false
      httpEnabled: true
      httpsEnabled: false
      tls:
        mode: SIMPLE
        credentialName: ""

  # Configuration parameters
  partnerIngestUrl: 'https://partner-{{ tpl .Values.global.registryHostname $ }}/partner/ingest_data?data_model=dcivc'
  vpPresentationId: "7f3c2a91-bb84-4d1f-9a6e-41c8e5a0d9f2"
  vpPurpose: "Digital identity verification for registry onboarding"
  # Browser-facing IAM URL (Staff Portal UI login providers)
  iamPublicUrl: 'https://staff-iam.{{ tpl .Values.global.baseDomain $ }}'

  envVars:
    # Per-release identifiers so two registry installs in the same namespace
    # don't share UI/audit identity. APPLICATION_MNEMONIC must equal the
    # Keycloak clientId verbatim (global.authClientId).
    APP_MNEMONIC: '{{ .Release.Name }}-ui'
    APP_URL: 'https://{{ tpl .Values.global.registryHostname $ }}'
    APPLICATION_MNEMONIC: '{{ tpl .Values.global.authClientId $ }}'
    # Allow profile/document images from MinIO pre-signed URLs in the browser CSP.
    CSP_SRC_IMG: 'self blob: data: https://{{ tpl .Values.global.minioHost $ }}'
    # Server-side calls from the Next.js Staff Portal UI pod to the Staff
    # Portal API stay inside the cluster — sibling K8s Service is named
    # "<release>-staff-portal-api" on port 80. Browser-side calls from the
    # client should use a separate NEXT_PUBLIC_* var pointing to the public
    # https://staff-<host> hostname (add when needed).
    BACKEND_API_URL: 'http://{{ .Release.Name }}-staff-portal-api'
    COOKIE_DOMAIN: '.{{ tpl .Values.global.baseDomain $ }}'
    IAM_URL: '{{ tpl .Values.iamPublicUrl $ }}'
    KEYCLOAK_LOGOUT_URL: '{{ tpl .Values.global.keycloakIssuerUrl $ }}/protocol/openid-connect/logout'
    LOGIN_PROVIDER_ID: '1'
    MASTERDATA_BACKEND_API_URL: '{{ .Values.global.masterDataApiUrl }}'
    NODE_ENV: production
    PARTNER_INGEST_URL: '{{ tpl .Values.partnerIngestUrl $ }}'
    REDIRECT_URL: 'https://{{ tpl .Values.global.registryHostname $ }}'
    VERIFY_SERVICE_URL: 'https://{{ tpl .Values.global.registryHostname $ }}/v1/verify'
    VP_CLIENT_ID: 'did:web:{{ tpl .Values.global.registryHostname $ }}:v1:verify'
    VP_PRESENTATION_ID: '{{ .Values.vpPresentationId }}'
    VP_PURPOSE: '{{ .Values.vpPurpose }}'

  envVarsFrom: {}

  startupProbe:
    enabled: true
    httpGet:
      path: /
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
    successThreshold: 1
  livenessProbe:
    enabled: true
    httpGet:
      path: /
      port: http
    initialDelaySeconds: 20
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1
  readinessProbe:
    enabled: true
    httpGet:
      path: /
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1

# ==========================================================================
# Agent Portal UI — the reference web client for VC issuance.
#
# The agent portal UI: find the beneficiary, have them authenticate at eSignet,
# download the printable credential. Off by default, like the API it talks to.
#
# Same shape as staffUi -- a Next.js server, not a static bundle. The server
# side is the BFF: it holds the httpOnly auth cookies IAM set and adds the
# backend's auth headers, so the browser never handles a token. Configured
# entirely from pod env (below), so one image serves every environment.
# ==========================================================================

agentPortalUi:
  enabled: false
  nameOverride: agent-portal-ui
  hostname: '{{ tpl .Values.global.agentPortalHostname $ }}'
  replicaCount: 1
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
  updateStrategy:
    type: RollingUpdate
  serviceAccount:
    create: false
    name: ""
  image:
    repository: openg2p/openg2p-registry-agent-ui
    tag: "1.0.0"
    pullPolicy: Always
  podSecurityContext:
    enabled: false
    fsGroup: 1001
  containerSecurityContext:
    enabled: false
    runAsUser: 1001
    runAsNonRoot: true

  service:
    type: ClusterIP
    port: 80
    externalTrafficPolicy: Cluster

  # nginx-unprivileged, which listens on 8080 — NOT 3000. This block was modelled
  # on staffUi, a Next.js app; leaving its port here pointed every probe at a
  # port nothing listens on, so the pod served fine and was restarted anyway.
  containerPort: 3000

  # Staff Portal UI is a Next.js SSR app. Node's V8 heap can balloon on cold
  # start (JIT warmup), large page pre-render, or concurrent SSR. Sizing
  # mirrored from NSR (~67Mi idle measured there): request 192Mi covers the
  # baseline with headroom; 768Mi limit is a generous cap matching typical
  # Next.js production sizing (no cpu limit — see staffApi rationale).
  resources:
    requests:
      cpu: 50m
      memory: 192Mi
    limits:
      memory: 768Mi

  istio:
    enabled: true
    virtualservice:
      enabled: true
      host: '{{ tpl .Values.global.agentPortalHostname $ }}'
      gateway: "internal"
      destination: '{{ .Release.Name }}-agent-portal-ui'
      destinationPort: '{{ .Values.service.port }}'
      prefix: '/'
      rewriteUri: "/"
    gateway:
      enabled: false
      host: '{{ tpl .Values.global.agentPortalHostname $ }}'
      ingressController:
        istio: ingressgateway
      httpTlsRedirect: false
      httpEnabled: true
      httpsEnabled: false
      tls:
        mode: SIMPLE
        credentialName: ""

  # The agent UI is a static SPA served by nginx: it takes NO build-time or
  # pod env configuration, read at request time by the Next.js server.
  envVars:
    # Same wiring as staffUi, because this is the same kind of app: a Next.js
    # server whose /api routes are the BFF. It holds the httpOnly auth cookies
    # and adds the backend's Authorization/Cookie/CSRF headers, so the browser
    # never handles a token.
    #
    # The Agent Portal API, in-cluster. The browser never calls it directly.
    BACKEND_API_URL: 'http://{{ .Release.Name }}-agent-portal-api'
    # Parent domain the IAM callback's Set-Cookie is rewritten to.
    COOKIE_DOMAIN: '.{{ tpl .Values.global.baseDomain $ }}'
    # iam-agent-portal-api: the OIDC confidential client for the agent realm.
    # NOT the staff IAM host -- that one serves the staff realm's provider.
    IAM_URL: '{{ tpl .Values.global.agentIamPublicUrl $ }}'
    # Row id of the agent realm in IAM's login_providers (staff is 1). Must
    # match iamAgentPortalApi.loginProviders[0].id in the IAM chart.
    LOGIN_PROVIDER_ID: '10'
    # Roles -> permissions resolution lives ONLY on iam-staff-portal-api, so the
    # UI asks the STAFF IAM for permissions even though login goes through the
    # agent IAM. Same split the Agent Portal API uses (auth_provider_api_url).
    AUTH_PROVIDER_API_URL: '{{ tpl (default .Values.global.iamServiceUrl .Values.global.agentAuthProviderApiUrl) $ }}'
    # Must equal the Keycloak client id the agent's roles sit under: IAM reads
    # resource_access[APPLICATION_MNEMONIC] and then looks for an IAM
    # application registered under that same mnemonic. The iam-register job
    # below is what registers it.
    APPLICATION_MNEMONIC: '{{ tpl .Values.global.agentKeycloakClientId $ }}'

  envVarsFrom: {}

  startupProbe:
    enabled: true
    httpGet:
      path: /
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
    successThreshold: 1
  livenessProbe:
    enabled: true
    httpGet:
      path: /
      port: http
    initialDelaySeconds: 20
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1
  readinessProbe:
    enabled: true
    httpGet:
      path: /
      port: http
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 6
    successThreshold: 1

celeryBeat:
  enabled: true
  nameOverride: celery-beat-producer
  replicaCount: 1
  image:
    repository: openg2p/openg2p-registry-celery
    tag: "1.2.1"
    pullPolicy: Always

  # Celery Beat Producer is NOT just a scheduler in this chart — it also
  # does batch enqueueing (REGISTRY_CELERY_BEAT_BATCH_SIZE=2000,
  # NO_OF_TASKS_TO_PROCESS=4 above). Each tick pulls up to 2000 records,
  # holds them in memory while creating up to 4 parallel task envelopes, and
  # dispatches to Redis. Sizing mirrored from NSR (~500Mi idle measured there):
  # request 640Mi reflects the real footprint; 1.5Gi limit is a generous cap
  # (no cpu limit — see staffApi rationale). Lower BATCH_SIZE or
  # NO_OF_TASKS_TO_PROCESS if you need to shrink the per-tick memory.
  resources:
    requests:
      cpu: 20m
      memory: 640Mi
    limits:
      memory: 1536Mi

  envVars:
    REGISTRY_CELERY_BEAT_DB_HOSTNAME: '{{ .Values.global.postgresqlHost }}'
    REGISTRY_CELERY_BEAT_DB_DBNAME: '{{ tpl .Values.global.registryDB $ }}'
    REGISTRY_CELERY_BEAT_DB_PORT: '{{ .Values.global.registryDBPort }}'
    REGISTRY_CELERY_BEAT_DB_USERNAME: '{{ tpl .Values.global.registryDBUser $ }}'
    # SQLAlchemy pool per beat process.
    REGISTRY_CELERY_BEAT_DB_POOL_SIZE: 5
    REGISTRY_CELERY_BEAT_DB_POOL_MAX_OVERFLOW: 10
    REGISTRY_CELERY_BEAT_DB_POOL_PRE_PING: true
    REGISTRY_CELERY_BEAT_DB_POOL_RECYCLE: 1800

    REGISTRY_CELERY_BEAT_CELERY_BROKER_URL: redis://{{ tpl .Values.global.redisInstallationName $ }}:6379/0
    REGISTRY_CELERY_BEAT_CELERY_BACKEND_URL: redis://{{ tpl .Values.global.redisInstallationName $ }}:6379/0

    REGISTRY_CELERY_BEAT_WORKER_QUEUE: registry_worker_queue
    REGISTRY_CELERY_BEAT_BATCH_SIZE: 2000
    REGISTRY_CELERY_BEAT_NO_OF_TASKS_TO_PROCESS: 4
    REGISTRY_CELERY_BEAT_EXPORT_NO_OF_TASKS_TO_PROCESS: '{{ .Values.global.exportNoOfTasksToProcess }}'

    REGISTRY_CELERY_BEAT_DATA_TRANSFORMATION_BEAT_PRODUCER_FREQUENCY: 20
    REGISTRY_CELERY_BEAT_INGEST_DATA_BEAT_PRODUCER_FREQUENCY: 20
    REGISTRY_CELERY_BEAT_INGEST_DATA_CLASSIFICATION_BEAT_PRODUCER_FREQUENCY: 20
    REGISTRY_CELERY_BEAT_OUTGEST_DATA_PUBLISH_BEAT_PRODUCER_FREQUENCY: 20
    REGISTRY_CELERY_BEAT_OUTGEST_TOPIC_REGISTER_BEAT_PRODUCER_FREQUENCY: 20
    REGISTRY_CELERY_BEAT_DEDUPLICATION_BEAT_PRODUCER_FREQUENCY: 30
    REGISTRY_CELERY_BEAT_REGISTER_EXPORT_BEAT_PRODUCER_FREQUENCY: '{{ .Values.global.exportBeatProducerFrequency }}'
    CELERY_APP: "openg2p_registry_celery_beat.main.celery_app"
    CELERY_OPTS: "worker --beat --loglevel=info --schedule=/tmp/celery-beat-schedule.db"

  envVarsFrom:
    REGISTRY_CELERY_BEAT_DB_PASSWORD:
      secretKeyRef:
        name: '{{ tpl .Values.global.registryDBSecret $ }}'
        key: '{{ tpl .Values.global.registryDBUserPasswordKey $ }}'

  postgresCheckerInit:
    enabled: true
    image:
      repository: jbergknoff/postgresql-client
      tag: latest
      pullPolicy: IfNotPresent
    command:
    - 'sh'
    args:
    - '-c'
    - >-
      until pg_isready
      -U ${REGISTRY_CELERY_BEAT_DB_USERNAME}
      -h ${REGISTRY_CELERY_BEAT_DB_HOSTNAME}
      -p ${REGISTRY_CELERY_BEAT_DB_PORT:-5432}
      -d ${REGISTRY_CELERY_BEAT_DB_DBNAME:-registrydb};
      do sleep 3;
      done
    containerSecurityContext:
      enabled: false

  serviceAccount:
    create: false
    name: ""

celeryWorker:
  enabled: true
  nameOverride: celery-worker
  replicaCount: 1
  image:
    repository: openg2p/openg2p-registry-celery
    tag: "1.2.1"
    pullPolicy: Always

  # Celery Worker is the largest variable-load component in the chart. It
  # processes async tasks (bulk partner ingest, data transformation,
  # classification) — memory spikes well past 1Gi when crunching large CSV
  # ingests or classifying big batches (2000-row batches held in memory).
  # Sizing mirrored from NSR (~600Mi idle measured there): request 1Gi reserves
  # the real footprint so the scheduler doesn't over-pack workers onto a node;
  # 2.5Gi limit is a generous runaway cap for the worst batch peak (no cpu
  # limit — see staffApi rationale). If ingests OOM at the cap, lower
  # REGISTRY_CELERY_WORKERS_BATCH_SIZE or scale out replicaCount.
  resources:
    requests:
      cpu: 100m
      memory: 1Gi
    limits:
      memory: 2560Mi

  envVars:
    REGISTRY_CELERY_WORKERS_DB_HOSTNAME: '{{ .Values.global.postgresqlHost }}'
    REGISTRY_CELERY_WORKERS_DB_DBNAME: '{{ tpl .Values.global.registryDB $ }}'
    REGISTRY_CELERY_WORKERS_DB_PORT: 5432
    REGISTRY_CELERY_WORKERS_DB_USERNAME: '{{ tpl .Values.global.registryDBUser $ }}'
    # SQLAlchemy pool per worker process (also used for master-data).
    REGISTRY_CELERY_WORKERS_DB_POOL_SIZE: 5
    REGISTRY_CELERY_WORKERS_DB_POOL_MAX_OVERFLOW: 10
    REGISTRY_CELERY_WORKERS_DB_POOL_PRE_PING: true
    REGISTRY_CELERY_WORKERS_DB_POOL_RECYCLE: 1800

    REGISTRY_CELERY_WORKERS_MASTER_DATA_API_URL: '{{ tpl .Values.global.masterDataApiUrl $ }}'
    REGISTRY_CELERY_WORKERS_MASTER_DATA_TOKEN_URL: "{{ tpl .Values.global.keycloakIssuerUrl $ }}/protocol/openid-connect/token"
    REGISTRY_CELERY_WORKERS_MASTER_DATA_CLIENT_ID: '{{ tpl .Values.global.pmSeedClientId $ }}'

    REGISTRY_CELERY_WORKERS_CELERY_BROKER_URL: "redis://{{ tpl .Values.global.redisInstallationName $ }}:6379/0"
    REGISTRY_CELERY_WORKERS_CELERY_BACKEND_URL: "redis://{{ tpl .Values.global.redisInstallationName $ }}:6379/0"
    REGISTRY_CELERY_WORKERS_WORKER_QUEUE: "registry_worker_queue"

    REGISTRY_CELERY_WORKERS_BATCH_SIZE: 2000
    REGISTRY_CELERY_WORKERS_WORKER_MAX_ATTEMPTS: 5
    REGISTRY_CELERY_WORKERS_EXPORT_BATCH_SIZE: '{{ .Values.global.exportBatchSize }}'
    REGISTRY_CELERY_WORKERS_EXPORT_WORKER_MAX_ATTEMPTS: '{{ .Values.global.exportWorkerMaxAttempts }}'
    REGISTRY_CELERY_WORKERS_EXPORT_PRESIGNED_URL_EXPIRY_HOURS: '{{ .Values.global.exportPresignedUrlExpiryHours }}'
    REGISTRY_CELERY_WORKERS_EXPORT_FILES_PREFIX: '{{ .Values.global.exportFilesPrefix }}'

    REGISTRY_CELERY_WORKERS_MINIO_ENDPOINT: '{{ tpl .Values.global.minioHost $ }}'
    REGISTRY_CELERY_WORKERS_MINIO_SECURE: '{{ .Values.global.minioSecure }}'
    REGISTRY_CELERY_WORKERS_MINIO_BUCKET_NAME: '{{ tpl .Values.global.defaultBucketName $ }}'
    REGISTRY_CELERY_WORKERS_TEMPLATE_BUCKET_NAME: '{{ tpl .Values.global.templateBucketName $ }}'
    REGISTRY_CELERY_WORKERS_FUNCTIONAL_ID_GENERATION_URL: '{{ tpl .Values.global.idGeneratorInternalUrl $ }}'
    REGISTRY_CELERY_WORKERS_ID_GENERATION_ALLOCATION_PATH: '/idgenerator/{id_type}/id'

    REGISTRY_CELERY_WORKERS_PARTNER_MGMT_API_URL: '{{ tpl .Values.global.partnerManagementApiUrl $ }}'
    REGISTRY_CELERY_WORKERS_PARTNER_MGMT_ADMIN_API_URL: '{{ tpl .Values.global.partnerManagementAdminApiUrl $ }}'
    REGISTRY_CELERY_WORKERS_PARTNER_MGMT_ADMIN_TOKEN_URL: "{{ tpl .Values.global.keycloakIssuerUrl $ }}/protocol/openid-connect/token"
    REGISTRY_CELERY_WORKERS_PARTNER_MGMT_ADMIN_CLIENT_ID: '{{ tpl .Values.global.pmSeedClientId $ }}'
    CELERY_APP: "openg2p_registry_celery_worker.main.celery_app"
    CELERY_OPTS: "worker -Q registry_worker_queue --loglevel=info"

  envVarsFrom:
    REGISTRY_CELERY_WORKERS_MINIO_ACCESS_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioAccessKey $ }}'
    REGISTRY_CELERY_WORKERS_MINIO_SECRET_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioSecretKey $ }}'
    REGISTRY_CELERY_WORKERS_MINIO_READ_ACCESS_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioReadonlyAccessKey $ }}'
    REGISTRY_CELERY_WORKERS_MINIO_READ_SECRET_KEY:
      secretKeyRef:
        name: '{{ tpl .Values.global.minioSecret $ }}'
        key: '{{ tpl .Values.global.minioReadonlySecretKey $ }}'
    REGISTRY_CELERY_WORKERS_DB_PASSWORD:
      secretKeyRef:
        name: '{{ tpl .Values.global.registryDBSecret $ }}'
        key: '{{ tpl .Values.global.registryDBUserPasswordKey $ }}'
    REGISTRY_CELERY_WORKERS_PARTNER_MGMT_ADMIN_CLIENT_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.pmSeedClientId $ }}'
        key: client_secret
        optional: true
    REGISTRY_CELERY_WORKERS_MASTER_DATA_CLIENT_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.pmSeedClientId $ }}'
        key: client_secret
        optional: true

  postgresCheckerInit:
    enabled: true
    image:
      repository: jbergknoff/postgresql-client
      tag: latest
      pullPolicy: IfNotPresent
    command:
    - 'sh'
    args:
    - '-c'
    - >-
      until pg_isready
      -U ${REGISTRY_CELERY_WORKERS_DB_USERNAME}
      -h ${REGISTRY_CELERY_WORKERS_DB_HOSTNAME}
      -p ${REGISTRY_CELERY_WORKERS_DB_PORT:-5432}
      -d ${REGISTRY_CELERY_WORKERS_DB_DBNAME:-registrydb};
      do sleep 3;
      done
    containerSecurityContext:
      enabled: false

  podSecurityContext:
    enabled: false
    fsGroup: 1001
  containerSecurityContext:
    enabled: false
    runAsUser: 1001
    runAsNonRoot: true
  serviceAccount:
    create: false
    name: ""

redis:
  enabled: true
  architecture: standalone
  master:
    persistence:
      enabled: false
  auth:
    enabled: false

keycloak-init:
  enabled: true
  keycloak:
    # Internal URL to reach Keycloak within the namespace
    url: 'http://commons-keycloak:80'
    user: 'admin'
    password: ''
    # Points to the Keycloak chart's auto-created admin secret (<commons-release-name>-keycloak)
    existingSecret: 'commons-keycloak'
    existingSecretKey: 'admin-password'
  suffix: '{{ .Release.Namespace }}'
  realms:
    # ── The `agent` realm ──────────────────────────────────────────────────
    # Created alongside `staff` and for the same reason: without it the agent
    # portal has nothing to authenticate against, and neither the agent UI nor
    # the agent API can be used at all. keycloak-init is idempotent, so this is
    # safe to re-run and safe if the realm already exists.
    #
    # ── The `agent` realm ──────────────────────────────────────────────────
    # The realm and its confidential `agent-portal` client are owned by
    # commons-services, exactly as `staff`/`staff-portal` are: the client is
    # confidential and iam-agent-portal-api is its only consumer, so the client
    # and the Secret holding its secret must be created by the release that
    # configures that API. Only the demo user is added here, mirroring the demo
    # staff users below.
    agent:
      users:
        # Demo agent, mirroring the demo staff users below. Real deployments
        # create their own agents and should change this password.
        #
        # NOTE: keycloak-init does not reset an existing user's password, and
        # the realm lives in the Keycloak DB (commons), which outlives a
        # registry reinstall. Changing this value only takes effect on a realm
        # that does not yet have this user.
        - username: agent
          firstName: Field
          lastName: Agent
          email: agent@email.com
          password: '{{ .Values.global.agentUserPassword }}'
          enabled: true
          clientRoleMappings:
            agent-portal:
              - "register:issue_credential"
              - "register:verify_credential"
    staff:
      clients:
        # Per-release client inside the shared `staff` realm. clientId is
        # templated so two registry installs in the same namespace each get
        # their own OIDC client (and their own client_secret Secret named
        # after the clientId — see global.authClientSecret above).
        - clientId: '{{ .Release.Name }}-staff-portal'
          name: Registry Staff Portal
          # Host-scoped redirect URIs — restricted to the Staff Portal UI's
          # public hostname. Covers all paths under it (root, OIDC callback,
          # post-login landing). NEVER use a bare "*" — it allows token
          # redirects to arbitrary hosts and is a known OIDC vulnerability.
          redirectUris:
            - 'https://{{ tpl .Values.global.registryHostname $ }}/*'
          clientRoles:
            # --- Operations roles ---
            - "Intake Officer"
            - "Intake Validator"
            - "Data Editor"
            - "Data Validator"
            - "Data Supervisor"
            - "Integration Manager"
            - "Operations Administrator"
            # --- Configuration roles ---
            - "Schema Designer"
            - "Integration Specialist"
            - "Reference Data Specialist"
            - "Technical Administrator"
      users:
        # IMPORTANT: It is assumed that admin has already been created as part
        # of commons layer
        - username: admin
          clientRoleMappings:
            # Key must equal the per-release clientId above. keycloak-init
            # renders this whole block through `tpl`, so the Go-template
            # expression in the YAML key is resolved at install time.
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: iam.admin
          firstName: IAM
          lastName: Admin
          email: iam.admin@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            iam-staff-ui:
              - "IAM_ADMIN"
            realm-management:
              - "manage-clients"
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: alex.carter
          firstName: Alex
          lastName: Carter
          email: alex.carter@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: maya.reed
          firstName: Maya
          lastName: Reed
          email: maya.reed@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: nina.patel
          firstName: Nina
          lastName: Patel
          email: nina.patel@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: omar.sheikh
          firstName: Omar
          lastName: Sheikh
          email: omar.sheikh@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: zara.khan
          firstName: Zara
          lastName: Khan
          email: zara.khan@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: chipo.banda
          firstName: Chipo
          lastName: Banda
          email: chipo.banda@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: tendai.moyo
          firstName: Tendai
          lastName: Moyo
          email: tendai.moyo@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: hana.bekele
          firstName: Hana
          lastName: Bekele
          email: hana.bekele@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: tesfaye.mekonnen
          firstName: Tesfaye
          lastName: Mekonnen
          email: tesfaye.mekonnen@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"
        - username: mwansa.tembo
          firstName: Mwansa
          lastName: Tembo
          email: mwansa.tembo@email.com
          password: '{{ .Values.global.aweApproverUserPassword }}'
          enabled: true
          clientRoleMappings:
            '{{ .Release.Name }}-staff-portal':
              - "Operations Administrator"
              - "Technical Administrator"

idgenerator:
  enabled: true
  global:
    idGeneratorDB: '{{ printf "%s_idgenerator" .Release.Name | replace "-" "_" }}'
    idGeneratorDBUser: '{{ printf "%s_idgenerator_user" .Release.Name | replace "-" "_" }}'
    idGeneratorDBSecret: '{{ .Release.Name }}-idgenerator'
    idGeneratorDBUserPasswordKey: '{{ .Release.Name }}-idgenerator-db-user'
  postgres-init:
    enabled: true
    nameOverride: idgen-pg-init
  idGenerator:
    enabled: true
    appConfig:
      # Functional-ID pools, one per register. These MUST match the registers the
      # deployed extension actually defines — this chart's default is the
      # REFERENCE registry (Individual + Household). A variant registry overrides
      # this block with its own register names.
      #
      # NB Helm MERGES maps from a parent chart and a `null` in a parent's
      # values.yaml does NOT delete a subchart default, so a variant that names
      # different registers inherits these too and provisions unused pools. Keep
      # this list to what the reference registry needs.
      idTypes:
        individual:
          idLength: 12
        household:
          idLength: 10
      sequenceLimit: 3
      repeatingLimit: 2
      repeatingBlockLimit: 2
      conjugativeEvenDigitsLimit: 3
      digitsGroupLimit: 5
      reverseDigitsGroupLimit: 5
      notStartWith: ["0", "1"]
      restrictedNumbers: []
      poolMinThreshold: 1000
      poolGenerationBatchSize: 5000
      poolCheckIntervalSeconds: 30
      exhaustionMaxAttempts: 1000
      subBatchSize: 10000

# ---------------------------------------------------------------------------
# AWE — Approval Workflow Engine (openg2p-awe subchart).
#
# Installed as part of the Registry platform. The subchart ships sensible
# defaults for the backend, UI, Keycloak clients (awe-admin-portal,
# awe-admin-resolver) and Istio routing — we only override what must differ
# when AWE runs UNDER the registry release rather than standalone:
#
#   1. enabled            — gates the dependency (Chart.yaml condition).
#   2. postgres-init      — distinct nameOverride so AWE's DB-provisioning
#                           Job + ServiceAccount don't collide with the
#                           registry's own postgres-init (both default to
#                           `<release>-postgres-init`).
#   3. keycloak-init      — distinct nameOverride so AWE's client-provisioning
#                           Job + ConfigMap don't collide with the registry's
#                           own keycloak-init (both default to
#                           `<release>-keycloak-init-<revision>`).
#
# AWE's DB/secret names are namespaced via the `awe*` globals above. Its
# Keycloak admin client IDs are release-scoped via the `aweAdmin*` globals
# above AND the keycloak-init realm override below — the openg2p-awe subchart
# hardcodes `awe-admin-portal` / `awe-admin-resolver` literals in its realm
# block (not the globals), so we re-declare the realm clients/users here with
# `{{ .Release.Name }}-` prefixes. keycloak-init runs `tpl` over this block,
# so the template expressions resolve at install time. This makes the created
# Keycloak clients, their client-secret Secret, and the role-mappings unique
# per registry instance in the shared `staff` realm.
# ---------------------------------------------------------------------------
# AWE is installed ONCE per environment by commons-services (shared instance).
# This registry no longer bundles the openg2p-awe subchart or provisions AWE's
# Keycloak clients — commons-services owns those. See the AWE globals block above
# for how this registry consumes the shared AWE and registers its callback secret.

# ---------------------------------------------------------------------------
# Sanity — in-cluster smoke + DCI/consent e2e for the partner-api.
# ---------------------------------------------------------------------------
# Runs automatically on every install/upgrade as a post-install/upgrade hook.
#   runE2e=false (default) → smoke only (partner-api liveness + DCI route). Needs
#     nothing else — no PM, no CM, no auth, no data created.
#   runE2e=true → also the signed DCI search round-trip. Seeds the shared sanity
#     partner into Partner Management (key) and the Consent Manager (binding +
#     policy) first, then signs a DCI envelope + consent JWS and asserts the
#     response is permitted and clamped. Needs reachable PM + CM + the seed
#     clients' Keycloak roles (partner_manager on PM, CONSENT_MANAGER_ADMIN on CM),
#     and the enforcement switches ON to exercise consent (global.*Enabled).
sanity:
  # On by default. A bare platform install carries no sample and no bulk data —
  # every dbSeed loader below is off — so the smoke suite is the ONLY thing that
  # says whether the deploy is actually serving. It creates nothing and needs
  # nothing beyond the registry itself, and questions.yaml has advertised this as
  # `default: true` all along; the value said otherwise, which is the bug.
  # runE2e stays off: the full round-trip needs PM, CM, AWE and Keycloak admin.
  enabled: true
  image:
    repository: openg2p/openg2p-registry-sanity-tests
    tag: 1.2.1
    pullPolicy: Always
  # In-cluster partner-api Service URL (no ingress path prefix), :80.
  partnerBaseUrl: 'http://{{ .Release.Name }}-partner-api'
  verifyTls: false
  runE2e: false
  # true (default) → the Job propagates pytest's exit code, so a failing suite
  #   fails the Job and therefore the install/upgrade. A sanity run that could not
  #   verify the registry must not be reported as a healthy deploy.
  # false → the Job always exits 0 (opt-out; read the pod logs for results). Use
  #   only where a red deploy is worse than an unverified one.
  #
  # NB this gates on FAILURES, not skips. Tests whose dependencies are not
  # configured (no commons-services on a bring-up install) still skip and stay
  # green; a dependency that IS configured and then breaks now fails.
  failOnError: true
  readinessTimeout: 180
  # The consent object's audience (== the CM binding audience) + the registry's
  # controller id (== data_controller). Kept stable so the binding is reused.
  # Variant-neutral: this is the PLATFORM's fixture, and every variant inherits
  # it. They were named for the farmer registry, which is where they were first
  # written, not because anything farmer-specific reads them.
  # ── Agent Portal / VC issuance ──────────────────────────────────────────
  # The VC tests skip unless the Agent Portal is enabled AND an agent password
  # is supplied: the agent realm is provisioned outside this chart, so the suite
  # cannot create its own identity there the way it does for staff.
  agentUsername: sanity-agent
  agentPassword: ""
  # National (foundational) ID the VC e2e issues against.
  vcNationalId: TEST_SANITY_VC_0001

  cmAudience: OPENG2P_SANITY_PARTNER
  controllerId: openg2p-sanity-controller
  # DCI envelope sender id — PARTNER_<UPPER> must equal the PM partner reference
  # (PARTNER_CM_SANITY), i.e. sender "cm_sanity".
  dciSenderId: cm_sanity
  regType: Individual
  regRecordType: 'spdci-extensions-dci:Individual'
  # What the DCI search matches on (ILIKE '%<text>%' against search_text). This
  # MUST be the sanity record's functional_record_id, NOT the SANITYE2E marker:
  # the change-request test runs first and its approval regenerates search_text
  # from the record's real columns, dropping the manually-stuffed marker. The
  # functional_record_id is a genuine search-text field, so it survives — and it
  # appears in no seeded sample data, so the e2e only ever matches its own record.
  # data_seed.py builds the record from this same value, so the two cannot drift.
  searchText: SANITY-INDIVIDUAL-0001
  # Scopes the seeded CM policy allows + the consent object requests.
  #
  # These MUST be top-level keys of THIS register's outgoing DCI template:
  # clamping is a strict allow-list over the rendered record's top-level keys, so
  # a scope naming anything else silently clamps every record to {}. There is no
  # sub-field granularity — a scope carries everything nested under it. The values
  # below are the reference Individual register's; a variant that ships a
  # different template must override them (farmer-registry does).
  dataScopes: "demographic_info"
  # Scopes deliberately NOT consented to; the e2e asserts these never come back.
  deniedScopes: "member_identifier"
  # The reference Individual register (must match g2p_register_definitions.sql).
  # Used to find the register's bound AWE change-request policy and to raise the
  # CR. NB the KEY is still named farmerRegisterId — renaming it means changing
  # the SANITY_FARMER_REGISTER_ID env in _helpers.tpl, cfg.farmer_register_id in
  # the sanity suite, and farmer-registry's override, so it is left for a
  # coordinated change rather than half-renamed here.
  farmerRegisterId: a0000000-0000-4000-8000-000000000001
  # UI coordinates the change-request payload carries. Optional — set if the
  # deployment's metadata requires a specific tab/section.
  crTabId: "individual_info_tab"
  crSectionId: "in_demographic_details"
  # AWE applies its decision via a webhook back to the registry, so the change
  # lands asynchronously — how long the e2e waits for it.
  aweSettleTimeout: 90
  # Audit Manager exposes NO query API (ingest + health only), so the audit
  # assertion reads its table directly. Defaults match the commons install;
  # host falls back to global.postgresqlHost.
  auditDbHost: ""
  auditDbPort: 5432
  auditDb: audit_manager
  auditDbUser: audit_manager_user
  # Audit Manager's DB password Secret (created by commons-services). NB: the
  # secret is named `audit-manager` and the key is `audit-manager-db-user` —
  # matching what the auditmanager deployment itself mounts.
  auditDbSecret: audit-manager
  auditDbUserPasswordKey: audit-manager-db-user
  auditTimeout: 60
  # Registry staff-portal-api — the change-request e2e logs in as REAL demo
  # users via the password grant (the release's Keycloak client is a browser
  # OIDC client with no service account). It reuses the users keycloak-init
  # already provisions with global.aweApproverUserPassword.
  staffBaseUrl: 'http://{{ .Release.Name }}-staff-portal-api'
  # The suite provisions its OWN Keycloak user (sanity-data-seed Job) rather
  # than borrowing the keycloak-init demo users: those are created with a
  # TEMPORARY password, so Keycloak forces UPDATE_PASSWORD and a password grant
  # fails with "Account is not fully set up". The seeded user gets a
  # non-temporary password, so it can log in non-interactively. It is a TEST
  # identity — delete the `sanity-e2e` user from the staff realm to remove it.
  staffUsername: sanity-e2e
  staffPassword: sanity-e2e-pass
  # Roles granted to the test user on global.authClientId — these expand to the
  # changeRequest:create / changeRequest:approve / register:view /
  # registerHistory:view permissions the change-request flow needs.
  staffRoles: "Operations Administrator,Technical Administrator"
  # Safety bound on the approval stage-walk (the shipped policy has 2 stages).
  maxApprovalRounds: 5
  # How long the change-request test keeps retrying the first permission-gated
  # call while the registry's permission path warms up just after install.
  authReadyTimeout: 120
  # AWE admin role granted to the sanity user (a client role on the shared AWE's
  # awe-admin-portal client) so it can approve every task on the change request
  # — the shipped policy stages are mode='all' and name demo approvers whose
  # temporary passwords the suite cannot use.
  aweAdminClientId: awe-admin-portal
  aweAdminRole: AWE_ADMIN
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
